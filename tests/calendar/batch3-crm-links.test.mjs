import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import test from'node:test';const read=p=>readFileSync(p,'utf8');
test('Calendar CRM option source covers full Batch 3 record set without creating records',()=>{const route=read('src/app/api/calendar/link-options/route.ts');for(const table of ["from('contacts')","from('leads')","from('quotes')","from('orders')","from('scheduled_tasks')","from('mail_threads')"])assert.match(route,new RegExp(table.replace(/[()']/g,m=>'\\'+m)));for(const type of ["'contact'","'buyer'","'supplier'","'task'","'lead'","'quote'","'order'","'mail_thread'"])assert.match(route,new RegExp(type));assert.match(route,/autonomousMutation:false/);});
