import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import test from'node:test';const read=p=>readFileSync(p,'utf8');
test('Mail CRM identity layer supports contacts buyer supplier and explicit thread links',()=>{const helper=read('src/lib/contacts/identity.ts'),migration=read('supabase/migrations/20260912015600_s41_batch3_contacts_crm_identity.sql'),links=read('src/app/api/mail/crm-links/route.ts');assert.match(helper,/from\('contacts'\)/);assert.match(helper,/from\('leads'\)/);assert.match(helper,/buyer/);assert.match(helper,/supplier/);assert.match(migration,/create table if not exists public\.mail_crm_links/);assert.match(links,/leadCreated:false/);assert.match(links,/mailbox_id/);});
