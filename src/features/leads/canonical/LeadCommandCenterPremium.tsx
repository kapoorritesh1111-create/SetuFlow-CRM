import Link from 'next/link';
import type { LeadProfileData } from '@/lib/queries/leads';
import { createLeadQuoteDraftFromLead } from '@/features/quotes/server/lead-draft-actions';
import { moveCanonicalLeadStage, reassignCanonicalLeadOwner, saveCanonicalLeadDetails, saveCanonicalQualificationMapping, scheduleCanonicalLeadFollowUp } from './actions';
import FollowUpComposer from './FollowUpComposer';

type TeamMember = { id: string; name: string; email?: string | null };
type Props = { data: LeadProfileData; canReassignOwner?: boolean; teamMembers?: TeamMember[]; backHref?: string };

const LOCKED = new Set(['accepted', 'rejected', 'expired', 'cancelled', 'declined']);
const LIFECYCLE = new Set(['sent', 'accepted', 'rejected', 'expired', 'cancelled', 'declined']);
const BUYER_STAGES = ['New Lead', 'Qualified', 'Contacted', 'Samples Sent', 'Negotiation', 'Won', 'Lost'];
const SUPPLIER_STAGES = ['New Supplier', 'Qualified', 'Contacted', 'Samples Sent', 'Negotiation', 'Won', 'Lost'];

function title(value?: string | null) { return String(value || 'draft').replace(/_/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase()); }
function normalize(value?: string | null) { return String(value || '').trim().toLowerCase().replace(/\s+/g, ' '); }
function initials(name?: string | null) { return String(name || 'Lead').split(/\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]?.toUpperCase()).join('') || 'LD'; }
function phoneForUrl(value?: string | null) { return String(value || '').replace(/[^+\d]/g, ''); }
function money(value?: number | null, cur = 'USD') { return typeof value === 'number' && Number.isFinite(value) ? `${cur} ${value.toLocaleString(undefined, { maximumFractionDigits: 2 })}` : '—'; }
function sortedQuotes(data: LeadProfileData) { return [...(data.quotes || [])].sort((a: any, b: any) => String(b.updated_at || b.created_at || '').localeCompare(String(a.updated_at || a.created_at || ''))); }
function quoteValue(quote: any) { return (quote?.lineItems || []).reduce((sum: number, line: any) => sum + Number(line.quantity || 0) * Number(line.unit_price || line.catalog_price_amount || 0), 0); }
function currency(data: LeadProfileData, quote?: any | null) { return quote?.display_currency || quote?.currency || data.lead?.deal_currency || 'USD'; }
function quoteHref(data: LeadProfileData, quote: any) { const status = normalize(quote?.status); return LIFECYCLE.has(status) ? `/quotes?status=${encodeURIComponent(status)}&mode=buyers&quoteId=${quote.id}` : `/leads/${data.lead!.id}/quote?quoteId=${quote.id}&step=1`; }
function activeStageName(data: LeadProfileData) { return data.stages.find((stage) => stage.id === data.lead?.stage_id)?.name || 'New Lead'; }
function nextActionLabel(quote: any | null) { const status = normalize(quote?.status); if (!quote) return 'Create quote'; if (status === 'sent') return 'Track buyer response'; if (status === 'accepted') return 'Create order / handoff'; if (status === 'approval_pending') return 'Review approval'; return 'Continue builder'; }
function readinessScore(data: LeadProfileData) { let score = 25; if (data.lead?.email || data.lead?.phone) score += 15; if (data.linkedProducts.length) score += 20; if (data.linkedMarkets.length) score += 15; if (data.quotes.length) score += 15; if (data.followUps.length) score += 10; return Math.min(100, score); }
function ownerName(data: LeadProfileData, members: TeamMember[]) { const ownerId = data.lead?.owner_user_id; return members.find((member) => member.id === ownerId)?.name || data.profiles.find((profile: any) => profile.id === ownerId)?.full_name || 'Unassigned'; }

function ShellIcon({ children, tone = 'blue' }: { children: string; tone?: 'blue' | 'emerald' | 'purple' | 'amber' }) {
  const bg = tone === 'emerald' ? 'bg-emerald-50 text-emerald-700' : tone === 'purple' ? 'bg-purple-50 text-purple-700' : tone === 'amber' ? 'bg-amber-50 text-amber-700' : 'bg-blue-50 text-blue-700';
  return <span className={`flex h-9 w-9 items-center justify-center rounded-2xl text-sm font-black ${bg}`}>{children}</span>;
}

function NewQuoteButton({ leadId, sourceQuoteId, label = 'Create New Quote', compact = false }: { leadId: string; sourceQuoteId?: string | null; label?: string; compact?: boolean }) {
  return <form action={createLeadQuoteDraftFromLead}><input type="hidden" name="lead_id" value={leadId} />{sourceQuoteId ? <input type="hidden" name="source_quote_id" value={sourceQuoteId} /> : null}<input type="hidden" name="force_new" value="true" /><button className={compact ? 'inline-flex h-9 items-center gap-1.5 whitespace-nowrap rounded-2xl border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-700 shadow-sm transition hover:border-blue-200 hover:text-blue-700' : 'inline-flex h-10 items-center gap-2 whitespace-nowrap rounded-2xl bg-blue-600 px-4 text-sm font-semibold text-white shadow-sm transition hover:bg-blue-700'}>+ {label}</button></form>;
}

function OwnerControl({ data, members, canReassign }: { data: LeadProfileData; members: TeamMember[]; canReassign?: boolean }) {
  const lead = data.lead!;
  if (!canReassign) return <span>Owner: <span className="font-semibold text-slate-700">{ownerName(data, members)}</span></span>;
  return <form id="lead-owner" action={reassignCanonicalLeadOwner} className="inline-flex items-center gap-1.5 rounded-full bg-slate-50 px-2 py-1"><input type="hidden" name="lead_id" value={lead.id} /><span className="text-slate-500">Owner:</span><select name="owner_user_id" defaultValue={lead.owner_user_id || ''} className="h-7 max-w-[150px] rounded-full border-0 bg-transparent px-1 text-xs font-semibold text-slate-700 outline-none"><option value="">Unassigned</option>{members.map((member) => <option key={member.id} value={member.id}>{member.name}</option>)}</select><button className="h-7 rounded-full bg-white px-2.5 text-[11px] font-semibold text-blue-700 shadow-sm ring-1 ring-slate-200">Save</button></form>;
}

function StageStrip({ data }: { data: LeadProfileData }) {
  const labels = normalize(data.lead?.lead_type) === 'supplier' ? SUPPLIER_STAGES : BUYER_STAGES;
  const current = data.stages.find((stage) => stage.id === data.lead?.stage_id) || null;
  const currentIndex = Math.max(0, labels.findIndex((label) => normalize(label) === normalize(current?.name)));
  const pipelineId = current?.pipeline_id || null;
  const stageMap = new Map<string, (typeof data.stages)[number]>();
  data.stages.filter((stage) => !pipelineId || stage.pipeline_id === pipelineId).forEach((stage) => { const key = normalize(stage.name); if (key && !stageMap.has(key)) stageMap.set(key, stage); });
  return <div id="stage-strip" className="w-full overflow-x-auto pb-1"><div className="grid min-w-[520px] grid-cols-7 items-start gap-1">{labels.map((label, index) => { const stage = stageMap.get(normalize(label)); const active = index === currentIndex; const done = index < currentIndex; return <form key={label} action={moveCanonicalLeadStage} className="text-center"><input type="hidden" name="lead_id" value={data.lead!.id} /><input type="hidden" name="stage_id" value={stage?.id || ''} /><button disabled={!stage} className="group w-full disabled:cursor-not-allowed"><span className={`mx-auto flex h-8 w-8 items-center justify-center rounded-full text-xs font-bold ${active ? 'bg-emerald-600 text-white ring-4 ring-emerald-100' : done ? 'bg-emerald-600 text-white' : 'bg-slate-100 text-slate-500'}`}>{done || active ? '✓' : index + 1}</span><span className={`mt-2 block text-[10px] font-bold ${active ? 'text-emerald-700' : 'text-slate-500'}`}>{label}</span></button></form>; })}</div></div>;
}

function SummaryCard({ label, value, helper, href, tone = 'blue' }: { label: string; value: string; helper: string; href?: string; tone?: 'blue' | 'emerald' | 'purple' | 'amber' }) {
  const inner = <div className="rounded-[1.35rem] border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-center gap-3"><ShellIcon tone={tone}>{tone === 'emerald' ? '□' : tone === 'purple' ? '↗' : tone === 'amber' ? '↳' : '◎'}</ShellIcon><div><p className="text-xs font-semibold text-slate-500">{label}</p><p className="mt-1 text-lg font-bold text-slate-950">{value}</p><p className="mt-1 text-xs font-semibold text-blue-600">{helper}</p></div></div></div>;
  return href ? <Link href={href}>{inner}</Link> : inner;
}

function QuotesList({ data, quotes }: { data: LeadProfileData; quotes: any[] }) {
  return <section className="rounded-[1.5rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><ShellIcon>Q</ShellIcon><h2 className="text-lg font-bold text-slate-950">Quotes on this Lead</h2></div><NewQuoteButton leadId={data.lead!.id} sourceQuoteId={quotes[0]?.id} label="Create New Quote" compact /></div><div className="mt-4 grid gap-4 md:grid-cols-2">{quotes.map((quote: any) => <Link key={quote.id} href={quoteHref(data, quote)} className="flex items-center justify-between gap-4 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:border-blue-200 hover:shadow-md"><div><p className="text-base font-bold text-slate-950">{quote.quote_number || `Quote ${quote.id.slice(0, 8)}`} · {title(quote.status)}</p><p className="mt-2 text-sm font-medium text-slate-600">{(quote.lineItems || []).length} products · {quoteValue(quote) ? money(quoteValue(quote), currency(data, quote)) : 'No value'}</p></div><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-bold text-amber-700">Builder</span></Link>)}</div></section>;
}

function GuruCard({ activeQuote }: { activeQuote: any | null }) {
  return <aside className="rounded-[1.5rem] border border-emerald-200 bg-emerald-50 p-5 shadow-sm"><div className="flex items-center gap-2"><span className="flex h-8 w-8 items-center justify-center rounded-xl bg-emerald-600 text-white">G</span><h3 className="font-bold text-slate-950">Setu Guru</h3></div><p className="mt-4 text-sm font-semibold text-slate-800">Recommended action</p><p className="mt-2 text-sm leading-6 text-slate-600">{normalize(activeQuote?.status) === 'sent' ? 'Track buyer response and schedule the right commercial follow-up.' : 'Keep qualification, quote builder, and follow-up in one path. Create a fresh quote only when scope or terms change.'}</p><Link href="/training" className="mt-4 inline-flex text-sm font-semibold text-emerald-700">Learn more →</Link></aside>;
}

function SecondaryPanels({ data }: { data: LeadProfileData }) {
  const lead = data.lead!;
  const selectedProductIds = new Set(data.linkedProducts.map((product) => product.id));
  const selectedMarketIds = new Set(data.linkedMarkets.map((market) => market.id));
  const categories = ((data as any).productCategories || []) as Array<{ id: string; name: string }>;
  const productsByCategory = (categoryId?: string | null) => data.products.filter((product: any) => product.category_id === categoryId);
  const inferredCategoryName = (categoryId?: string | null) => {
    const known = categories.find((category) => category.id === categoryId)?.name;
    if (known) return known;
    const names = productsByCategory(categoryId).map((product: any) => String(product.name || '').toLowerCase());
    if (!names.length) return 'Product Category';
    if (names.every((name) => name.includes('powder'))) return 'Powders';
    if (names.some((name) => name.includes('chips') || name.includes('crisp'))) return 'Chips & Snacks';
    if (names.some((name) => name.includes('jaggery') || name.includes('sugar') || name.includes('honey'))) return 'Natural Sweeteners';
    const first = String(productsByCategory(categoryId)[0]?.name || 'Product').replace(/\b(chips|chip|powder|crisp|crisps)\b/gi, '').trim();
    return first ? `${first} Category` : 'Product Category';
  };
  const groupedCategories = Array.from(new Set(data.products.map((product: any) => product.category_id).filter(Boolean))).map((categoryId: string) => ({ id: categoryId, name: inferredCategoryName(categoryId), count: productsByCategory(categoryId).length }));
  return <section className="grid gap-4 xl:grid-cols-2"><details id="qualification" open className="rounded-[1.35rem] border border-slate-200 bg-white p-4 shadow-sm"><summary className="cursor-pointer list-none"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-blue-600">Qualification & Mapping</p><h3 className="mt-1 text-lg font-semibold text-slate-950">{data.linkedProducts.length} products · {data.linkedMarkets.length} markets</h3></summary><form action={saveCanonicalQualificationMapping} className="mt-4 grid gap-4 border-t border-slate-100 pt-4"><input type="hidden" name="lead_id" value={lead.id} /><div className="grid gap-4 lg:grid-cols-2"><div className="rounded-2xl border border-slate-100 bg-slate-50 p-3"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-slate-400">Pick categories</p><div className="mt-3 grid max-h-44 gap-2 overflow-auto pr-1">{groupedCategories.map((category) => <label key={category.id} className="flex items-center justify-between gap-3 rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-700 shadow-sm"><span>{category.name} <span className="text-xs text-slate-400">({category.count})</span></span><input type="checkbox" name="category_ids" value={category.id} /></label>)}{!groupedCategories.length ? <p className="text-sm font-medium text-slate-500">No categories available.</p> : null}</div></div><div className="rounded-2xl border border-slate-100 bg-slate-50 p-3"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-slate-400">Pick products</p><div className="mt-3 grid max-h-44 gap-2 overflow-auto pr-1">{data.products.map((product: any) => <label key={product.id} className="flex items-center justify-between gap-3 rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-700 shadow-sm"><span>{product.name}<span className="ml-2 text-xs text-slate-400">{product.sku || inferredCategoryName(product.category_id)}</span></span><input type="checkbox" name="product_ids" value={product.id} defaultChecked={selectedProductIds.has(product.id)} /></label>)}</div></div></div><div className="rounded-2xl border border-slate-100 bg-slate-50 p-3"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-slate-400">Pick markets</p><div className="mt-3 flex flex-wrap gap-2">{data.markets.map((market) => <label key={market.id} className="inline-flex items-center gap-2 rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-700 shadow-sm"><input type="checkbox" name="market_ids" value={market.id} defaultChecked={selectedMarketIds.has(market.id)} />{market.name}</label>)}</div></div><label className="grid gap-2 text-xs font-semibold uppercase tracking-[0.12em] text-slate-400">Qualification notes<textarea name="qualification_notes" defaultValue={data.workflow?.qualificationNotes ?? lead.notes ?? ''} className="min-h-20 rounded-xl border border-slate-200 px-3 py-2 text-sm font-medium normal-case tracking-normal text-slate-700" /></label><button className="w-fit rounded-2xl bg-emerald-600 px-4 py-3 text-sm font-semibold text-white">Save qualification & mapping</button></form></details><details id="edit-lead" className="rounded-[1.35rem] border border-slate-200 bg-white p-4 shadow-sm"><summary className="cursor-pointer list-none"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-blue-600">Quick Edit</p><h3 className="mt-1 text-lg font-semibold text-slate-950">Lead, contact, address, deal value</h3></summary><form action={saveCanonicalLeadDetails} className="mt-4 grid gap-3 border-t border-slate-100 pt-4 md:grid-cols-2"><input type="hidden" name="lead_id" value={lead.id} />{[['company_name', lead.company_name, 'Company'], ['contact_name', lead.contact_name || '', 'Contact name'], ['email', lead.email || '', 'Email'], ['phone', lead.phone || '', 'Phone'], ['whatsapp_number', lead.whatsapp_number || '', 'WhatsApp'], ['country', lead.country || '', 'Country'], ['deal_value', String(lead.deal_value || ''), 'Deal value'], ['deal_currency', lead.deal_currency || 'USD', 'Currency']].map(([name, value, label]) => <label key={name} className="grid gap-1 text-xs font-semibold uppercase tracking-[0.12em] text-slate-400">{label}<input name={name} defaultValue={value} className="rounded-xl border border-slate-200 px-3 py-2 text-sm font-medium normal-case tracking-normal text-slate-700" /></label>)}<label className="grid gap-1 text-xs font-semibold uppercase tracking-[0.12em] text-slate-400 md:col-span-2">Notes / address<textarea name="notes" defaultValue={lead.notes || ''} className="min-h-20 rounded-xl border border-slate-200 px-3 py-2 text-sm font-medium normal-case tracking-normal text-slate-700" /></label><button className="w-fit rounded-2xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white">Save changes</button></form></details></section>;
}

export default function LeadCommandCenterPremium({ data, canReassignOwner = false, teamMembers = [], backHref = '/leads' }: Props) {
  const lead = data.lead!;
  const quotes = sortedQuotes(data);
  const activeQuote = quotes[0] || null;
  const activeValue = Number(lead.deal_value || 0) || quoteValue(activeQuote) || null;
  const activeStatus = normalize(activeQuote?.status);
  const primaryHref = activeQuote ? quoteHref(data, activeQuote) : '';
  const sharePriceListHref = `/leads/${lead.id}/share-price-list`;
  const members = teamMembers.length ? teamMembers : data.profiles.map((profile: any) => ({ id: profile.id, name: profile.full_name || profile.username || 'Team member' }));
  const whatsAppValue = lead.whatsapp_number || lead.phone;
  return <div className="mx-auto max-w-[1560px] space-y-4 pb-48"><section className="rounded-[1.65rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-5 xl:grid-cols-[minmax(0,0.42fr)_minmax(0,0.58fr)] xl:items-center"><div className="flex min-w-0 gap-4"><div className="flex h-16 w-16 shrink-0 items-center justify-center rounded-full bg-emerald-50 text-xl font-bold text-slate-800">{initials(lead.company_name)}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-3"><h2 className="text-2xl font-bold tracking-tight text-slate-950">{lead.company_name}</h2><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs font-semibold text-emerald-700">{title(lead.lead_type)}</span></div><div className="mt-2 flex flex-wrap items-center gap-x-3 gap-y-2 text-xs font-medium text-slate-600"><OwnerControl data={data} members={members} canReassign={canReassignOwner} /><span className="text-slate-300">|</span><span>Source: {lead.source_label || lead.source_type || 'Manual'}</span><span className="text-slate-300">|</span><span>Market: {data.linkedMarkets.map((market) => market.name).join(', ') || lead.country || '—'}</span></div><div className="mt-4 flex flex-wrap gap-2.5"><a href={`mailto:${lead.email || ''}`} className="inline-flex h-9 items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 text-xs font-medium text-slate-700 shadow-sm">✉ {lead.email || 'No email'}</a><a href={`tel:${phoneForUrl(lead.phone)}`} className="inline-flex h-9 items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 text-xs font-medium text-slate-700 shadow-sm">☎ {lead.phone || 'No phone'}</a><a href={`https://wa.me/${phoneForUrl(whatsAppValue).replace('+', '')}`} className="inline-flex h-9 items-center gap-2 rounded-xl border border-emerald-200 bg-white px-3 text-xs font-semibold text-emerald-700 shadow-sm">💬 {whatsAppValue || 'Chat on WhatsApp'}</a></div></div></div><StageStrip data={data} /></div></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><SummaryCard label="Readiness Score" value={`${readinessScore(data)} / 100`} helper="Excellent" /><SummaryCard label="Active Quote" value={activeQuote ? `${activeQuote.quote_number || 'Quote'} · ${title(activeQuote.status)}` : 'No quote'} helper="View Quote" href={activeQuote ? quoteHref(data, activeQuote) : undefined} tone="purple" /><SummaryCard label="Products Selected" value={`${data.linkedProducts.length} selected`} helper="Manage Products" href="#qualification" tone="emerald" /><SummaryCard label="Next Action" value={nextActionLabel(activeQuote)} helper="View Tasks" href="/tasks" tone="amber" /></section><section id="work" className="grid gap-4 xl:grid-cols-[minmax(0,0.42fr)_minmax(0,0.58fr)]"><div id="follow-up" className="rounded-[1.5rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><ShellIcon>↗</ShellIcon><h2 className="text-lg font-bold text-slate-950">Next Touchpoint</h2></div><FollowUpComposer leadId={lead.id} clientName={lead.company_name} senderName="Ritesh Kapoor" senderCompany="SETU Flow CRM" email={lead.email} whatsapp={lead.whatsapp_number || lead.phone} action={scheduleCanonicalLeadFollowUp} /></div><div id="commercial" className="relative rounded-[1.5rem] border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div className="flex min-w-0 items-start gap-3"><ShellIcon>↘</ShellIcon><div><h2 className="text-[17px] font-bold leading-6 text-slate-950">Commercial</h2><p className="mt-1 text-[15px] font-bold leading-5 text-slate-950">{activeQuote ? `${activeQuote.quote_number || 'Quote'} · ${title(activeQuote.status)}` : 'No quote yet'}</p><p className="mt-1 text-[13px] font-medium leading-5 text-slate-500">Open lifecycle, export PDF, or create a fresh quote from this commercial context.</p></div></div><div className="flex flex-wrap justify-end gap-2">{primaryHref ? <Link href={primaryHref} className="inline-flex h-9 items-center rounded-2xl bg-blue-600 px-3 text-xs font-semibold text-white shadow-sm">Open Builder</Link> : null}<Link href={sharePriceListHref} className="inline-flex h-9 items-center rounded-2xl border border-emerald-200 bg-emerald-50 px-3 text-xs font-semibold text-emerald-700 shadow-sm">Share Price List</Link>{activeQuote ? <Link href={`/api/quotes/${activeQuote.id}/pdf`} className="inline-flex h-9 items-center rounded-2xl border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-700 shadow-sm">Customer PDF</Link> : null}<NewQuoteButton leadId={lead.id} sourceQuoteId={activeQuote?.id} label="New Quote" compact /></div></div><div className="mt-3 grid gap-3 md:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-slate-400">Buyer</p><dl className="mt-3 grid gap-2 text-sm"><div className="grid grid-cols-[90px_1fr]"><dt className="text-slate-500">Stage</dt><dd className="font-semibold text-slate-700">{activeStageName(data)}</dd></div><div className="grid grid-cols-[90px_1fr]"><dt className="text-slate-500">Deal Value</dt><dd className="font-semibold text-slate-700">{money(activeValue, currency(data, activeQuote))}</dd></div><div className="grid grid-cols-[90px_1fr]"><dt className="text-slate-500">Market</dt><dd className="font-semibold text-slate-700">{data.linkedMarkets.map((market) => market.name).join(', ') || lead.country || '—'}</dd></div></dl></div><div className="rounded-2xl border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-blue-600">Quotes</p><div className="mt-3 grid gap-1 text-sm font-semibold text-blue-900"><p>{quotes.length} total</p><p>{quotes.filter((quote: any) => LOCKED.has(normalize(quote.status))).length} locked</p><p>{quotes.filter((quote: any) => !LOCKED.has(normalize(quote.status))).length} open</p></div></div></div><div className="mt-3"><p className="text-sm font-semibold text-slate-700">Mapped Coverage</p><div className="mt-2 flex flex-wrap gap-2">{data.linkedProducts.slice(0, 6).map((product) => <span key={product.id} className="rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-700">{product.name}</span>)}</div></div></div></section><section className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_280px]"><QuotesList data={data} quotes={quotes} /><GuruCard activeQuote={activeQuote} /></section><SecondaryPanels data={data} /><div className="fixed bottom-3 left-[calc(8rem+1rem)] right-4 z-30 hidden rounded-[1.35rem] border border-slate-200 bg-white/95 p-2.5 shadow-2xl backdrop-blur md:flex md:items-center md:justify-between"><div className="flex flex-wrap gap-2.5"><Link href={backHref} className="inline-flex h-10 items-center rounded-2xl border border-slate-200 bg-white px-4 text-sm font-semibold text-slate-700">← Back to Leads</Link>{primaryHref ? <Link href={primaryHref} className="inline-flex h-10 items-center rounded-2xl bg-emerald-600 px-5 text-sm font-semibold text-white">Open Builder</Link> : <NewQuoteButton leadId={lead.id} />}<Link href={sharePriceListHref} className="inline-flex h-10 items-center rounded-2xl bg-blue-600 px-5 text-sm font-semibold text-white">Share Price List</Link>{normalize(activeQuote?.status) !== 'sent' && !LOCKED.has(activeStatus) ? <Link href={activeQuote ? `/leads/${lead.id}/quote?quoteId=${activeQuote.id}&mode=revise` : `/leads/${lead.id}/quote`} className="inline-flex h-10 items-center rounded-2xl border border-slate-200 bg-white px-4 text-sm font-semibold text-slate-700">Edit / Revise</Link> : null}<NewQuoteButton leadId={lead.id} sourceQuoteId={activeQuote?.id} label="New Quote" compact /></div><span className="rounded-full bg-slate-100 px-4 py-2 text-xs font-semibold uppercase tracking-[0.16em] text-blue-600">SETU Flow <span className="text-emerald-500">●</span></span></div></div>;
}
