'use client';import{useEffect,useState}from'react';import Link from'next/link';type E={id:string;title:string;starts_at:string;ends_at:string;meeting_provider:string;meeting_url?:string|null;location?:string|null};export function MobileCalendarWorkspace(){const[events,setEvents]=useState<E[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/calendar',{cache:'no-store'}).then(r=>r.json()).then(j=>setEvents(j.events??[])).finally(()=>setLoading(false))},[]);const groups=events.reduce((a:any,e)=>{const k=new Date(e.starts_at).toLocaleDateString(undefined,{weekday:'long',month:'short',day:'numeric'});(a[k]??=[]).push(e);return a},{});return <div className="min-h-screen bg-slate-50 pb-24"><header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 px-4 py-4 backdrop-blur"><div className="flex items-center justify-between"><div><div className="text-[10px] font-black uppercase tracking-[.18em] text-blue-600">Setu Communications</div><h1 className="text-xl font-black text-slate-950">Calendar</h1></div><Link href="/calendar?compose=1" className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-950 text-xl font-black text-white">+</Link></div></header><main className="space-y-6 p-4">{loading?<div className="py-16 text-center text-sm font-bold text-slate-400">Loading schedule…</div>:Object.keys(groups).length?Object.entries(groups).map(([day,list]:any)=><section key={day}><div className="mb-2 text-xs font-black uppercase tracking-wider text-slate-400">{day}</div><div className="space-y-2">{list.map((e:E)=><article key={e.id} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex gap-3"><div className="w-16 text-sm font-black text-slate-900">{new Date(e.starts_at).toLocaleTimeString(undefined,{hour:'numeric',minute:'2-digit'})}</div><div className="min-w-0 flex-1"><h2 className="truncate font-black text-slate-950">{e.title}</h2><p className="mt-1 text-xs text-slate-500">{e.location|| (e.meeting_provider==='zoom'?'Zoom meeting':'Calendar event')}</p>{e.meeting_url?<a href={e.meeting_url} target="_blank" rel="noreferrer" className="mt-3 inline-flex h-9 items-center rounded-xl bg-blue-600 px-4 text-xs font-black text-white">Join meeting</a>:null}</div></div></article>)}</div></section>):<div className="py-20 text-center"><h2 className="text-lg font-black text-slate-800">Your schedule is clear</h2><p className="mt-1 text-sm text-slate-400">Create a meeting when you're ready.</p></div>}</main></div>}
