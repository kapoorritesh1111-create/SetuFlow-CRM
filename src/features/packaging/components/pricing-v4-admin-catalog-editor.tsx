import {
  savePackagingChargeMasterV4,
  savePackagingCostMasterV4,
  savePackagingProductVariationV4,
  savePackagingServiceFamilyV4,
} from '@/features/packaging/server/pricing-v4-admin-catalog-actions';

const YELLOW='rounded-lg border border-yellow-300 bg-yellow-50 px-2.5 py-2 text-sm text-slate-900 outline-none ring-yellow-200 focus:ring-2';
const CHECK='flex items-center gap-2 rounded-lg border border-yellow-300 bg-yellow-50 px-3 py-2 text-xs font-medium text-slate-700';

function Section({title,description,count,children}:{title:string;description:string;count:number;children:React.ReactNode}){
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex flex-wrap items-start justify-between gap-3"><div><h3 className="text-lg font-bold text-slate-900">{title}</h3><p className="text-sm text-slate-500">{description}</p></div><span className="rounded-full border border-slate-200 bg-slate-50 px-2.5 py-1 text-xs font-semibold text-slate-600">{count}</span></div>{children}</section>;
}
function SaveButton({label='Save'}:{label?:string}){return <button className="rounded-lg bg-slate-900 px-3 py-2 text-xs font-semibold text-white hover:bg-slate-800">{label}</button>;}
function FamilyChecks({families,selected}:{families:any[];selected:Set<string>}){return <div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-3">{families.map((family:any)=><label key={family.id} className={CHECK}><input type="checkbox" name="family_ids" value={family.id} defaultChecked={selected.has(family.id)}/><span>{family.name}</span></label>)}</div>;}

function FamilyForm({family}:{family?:any}){
  return <form action={savePackagingServiceFamilyV4} className="grid gap-3 md:grid-cols-2 xl:grid-cols-4">{family?.id&&<input type="hidden" name="id" value={family.id}/>}<label><span className="mb-1 block text-xs font-semibold text-slate-600">Name</span><input name="name" defaultValue={family?.name??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Slug</span><input name="slug" defaultValue={family?.slug??''} placeholder="Auto from name" className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Product setup</span><select name="product_setup_mode" defaultValue={family?.product_setup_mode??'approved_sizes'} className={`${YELLOW} w-full`}><option value="approved_sizes">Approved sizes</option><option value="custom_dimensions">Custom dimensions</option><option value="both">Both</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Pricing engine</span><select name="pricing_engine_type" defaultValue={family?.pricing_engine_type??'service_formula'} className={`${YELLOW} w-full`}><option value="sup_formula">SUP formula</option><option value="matrix_per_frame">Matrix per frame</option><option value="service_formula">Service formula</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Default UOM</span><input name="default_uom" defaultValue={family?.default_uom??'pcs'} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Sort order</span><input name="sort_order" type="number" min="0" step="1" defaultValue={family?.sort_order??0} className={`${YELLOW} w-full`}/></label><label className={`${CHECK} self-end`}><input type="checkbox" name="is_active" defaultChecked={family?.is_active??true}/><span>Active</span></label><label className="md:col-span-2 xl:col-span-4"><span className="mb-1 block text-xs font-semibold text-slate-600">Description</span><textarea name="description" defaultValue={family?.description??''} rows={2} className={`${YELLOW} w-full`}/></label><div className="md:col-span-2 xl:col-span-4"><SaveButton label={family?'Save family':'Add family'}/></div></form>;
}

function VariationForm({variation,families}:{variation?:any;families:any[]}){
  return <form action={savePackagingProductVariationV4} className="grid gap-3 md:grid-cols-3 xl:grid-cols-5">{variation?.id&&<input type="hidden" name="id" value={variation.id}/>}<label><span className="mb-1 block text-xs font-semibold text-slate-600">Family</span><select name="family_id" defaultValue={variation?.family_id??families[0]?.id??''} className={`${YELLOW} w-full`}>{families.map((f:any)=><option key={f.id} value={f.id}>{f.name}</option>)}</select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Variation key</span><input name="variation_key" defaultValue={variation?.variation_key??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Name</span><input name="name" defaultValue={variation?.name??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Capacity label</span><input name="capacity_label" defaultValue={variation?.capacity_label??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Approval</span><select name="approval_state" defaultValue={variation?.approval_state??'draft'} className={`${YELLOW} w-full`}><option value="draft">Draft</option><option value="approved">Approved</option><option value="archived">Archived</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Width mm</span><input name="width_mm" type="number" min="0.001" step="0.001" defaultValue={variation?.width_mm??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Height mm</span><input name="height_mm" type="number" min="0.001" step="0.001" defaultValue={variation?.height_mm??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Bottom gusset each mm</span><input name="bottom_gusset_each_mm" type="number" min="0" step="0.001" defaultValue={variation?.bottom_gusset_each_mm??0} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Sort order</span><input name="sort_order" type="number" min="0" step="1" defaultValue={variation?.sort_order??0} className={`${YELLOW} w-full`}/></label><div className="self-end rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs text-slate-500"><div className="font-semibold text-slate-600">Dimension label</div><div>{variation?.dimension_label??'Calculated from dimensions'}</div></div><label className={CHECK}><input type="checkbox" name="is_active" defaultChecked={variation?.is_active??true}/><span>Active</span></label><label className={CHECK}><input type="checkbox" name="is_quoteable" defaultChecked={variation?.is_quoteable??false}/><span>Quoteable when approved</span></label><div className="md:col-span-3 xl:col-span-5"><SaveButton label={variation?'Save size':'Add size'}/></div></form>;
}

function CostForm({item,families,selected}:{item?:any;families:any[];selected:Set<string>}){
  return <form action={savePackagingCostMasterV4} className="grid gap-3 md:grid-cols-3 xl:grid-cols-5">{item?.id&&<input type="hidden" name="id" value={item.id}/>}<label><span className="mb-1 block text-xs font-semibold text-slate-600">Code</span><input name="code" defaultValue={item?.code??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Name</span><input name="name" defaultValue={item?.name??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Type</span><select name="item_type" defaultValue={item?.item_type??'material'} className={`${YELLOW} w-full`}><option value="material">Material</option><option value="process">Process</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Rate basis</span><select name="rate_basis" defaultValue={item?.rate_basis??'per_kg'} className={`${YELLOW} w-full`}><option value="per_kg">Per kg</option><option value="per_running_metre">Per running metre</option><option value="per_frame">Per frame</option><option value="per_unit">Per unit</option><option value="flat">Flat</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Current rate</span><input name="current_rate" type="number" min="0" step="0.001" defaultValue={item?.current_rate??''} placeholder="Needs rate" className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Rate UOM</span><input name="rate_uom" defaultValue={item?.rate_uom??'kg'} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Currency</span><input name="currency" defaultValue={item?.currency??'INR'} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Micron</span><input name="micron" type="number" min="0.001" step="0.001" defaultValue={item?.micron??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">GSM</span><input name="gsm" type="number" min="0" step="0.001" defaultValue={item?.gsm??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Density</span><input name="density" type="number" min="0.001" step="0.001" defaultValue={item?.density??''} className={`${YELLOW} w-full`}/></label><label className="md:col-span-3 xl:col-span-5"><span className="mb-1 block text-xs font-semibold text-slate-600">Specification</span><input name="specification" defaultValue={item?.specification??''} className={`${YELLOW} w-full`}/></label><div className="md:col-span-3 xl:col-span-5"><div className="mb-1 text-xs font-semibold text-slate-600">Available for Service Families</div><FamilyChecks families={families} selected={selected}/></div><label className={CHECK}><input type="checkbox" name="is_active" defaultChecked={item?.is_active??true}/><span>Active</span></label><div className="md:col-span-3 xl:col-span-5"><SaveButton label={item?'Save Master':'Add Master'}/></div></form>;
}

function ChargeForm({item,families,selected}:{item?:any;families:any[];selected:Set<string>}){
  return <form action={savePackagingChargeMasterV4} className="grid gap-3 md:grid-cols-3 xl:grid-cols-5">{item?.id&&<input type="hidden" name="id" value={item.id}/>}<label><span className="mb-1 block text-xs font-semibold text-slate-600">Code</span><input name="code" defaultValue={item?.code??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Name</span><input name="name" defaultValue={item?.name??''} className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Category</span><select name="category" defaultValue={item?.category??'extra'} className={`${YELLOW} w-full`}><option value="extra">Extra</option><option value="pre">Pre-production</option><option value="post">Post-production</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Basis</span><select name="basis" defaultValue={item?.basis??''} className={`${YELLOW} w-full`}><option value="">Needs basis</option><option value="per_unit">Per unit</option><option value="per_running_metre">Per running metre</option><option value="per_frame">Per frame</option><option value="flat">Flat</option><option value="percent">Percent</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Application stage</span><select name="application_stage" defaultValue={item?.application_stage??''} className={`${YELLOW} w-full`}><option value="">Needs stage</option><option value="before_wastage_margin">Before waste & margin</option><option value="after_core_price">After core price</option><option value="separate_quote_line">Separate quote line</option></select></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Current rate</span><input name="current_rate" type="number" min="0" step="0.001" defaultValue={item?.current_rate??''} placeholder="Needs rate" className={`${YELLOW} w-full`}/></label><label><span className="mb-1 block text-xs font-semibold text-slate-600">Currency</span><input name="currency" defaultValue={item?.currency??'INR'} className={`${YELLOW} w-full`}/></label><div className="md:col-span-3 xl:col-span-5"><div className="mb-1 text-xs font-semibold text-slate-600">Available for Service Families</div><FamilyChecks families={families} selected={selected}/></div><label className={CHECK}><input type="checkbox" name="is_active" defaultChecked={item?.is_active??true}/><span>Active</span></label><div className="md:col-span-3 xl:col-span-5"><SaveButton label={item?'Save charge':'Add charge'}/></div></form>;
}

export default function PricingV4AdminCatalogEditor({data}:{data:any}){
  const {families,variations,costs,charges,costLinks,chargeLinks}=data;
  const costFamilies=(id:string)=>new Set(costLinks.filter((link:any)=>link.cost_master_item_id===id).map((link:any)=>link.family_id));
  const chargeFamilies=(id:string)=>new Set(chargeLinks.filter((link:any)=>link.charge_master_item_id===id).map((link:any)=>link.family_id));
  return <div className="space-y-6">
    <Section title="Service Families" description="Create or edit the product setup and pricing-engine assignment." count={families.length}><details className="mb-3 rounded-xl border border-yellow-200 bg-yellow-50/40 p-3"><summary className="cursor-pointer text-sm font-bold text-slate-800">+ Add Service Family</summary><div className="mt-3"><FamilyForm/></div></details><div className="space-y-2">{families.map((family:any)=><details key={family.id} className="rounded-xl border border-slate-200"><summary className="flex cursor-pointer items-center justify-between gap-3 px-4 py-3"><div><div className="font-semibold text-slate-900">{family.name}</div><div className="text-xs text-slate-500">{family.product_setup_mode??'Legacy'} · {family.pricing_engine_type??family.pricing_mode}</div></div><span className="text-xs font-semibold text-slate-500">Edit</span></summary><div className="border-t border-slate-200 p-4"><FamilyForm family={family}/></div></details>)}</div></Section>
    <Section title="Product Sizes" description="Physical product data is editable; the dimension label is calculated and read-only." count={variations.length}><details className="mb-3 rounded-xl border border-yellow-200 bg-yellow-50/40 p-3"><summary className="cursor-pointer text-sm font-bold text-slate-800">+ Add Product Size</summary><div className="mt-3"><VariationForm families={families}/></div></details><div className="space-y-2">{variations.map((variation:any)=><details key={variation.id} className="rounded-xl border border-slate-200"><summary className="flex cursor-pointer items-center justify-between gap-3 px-4 py-3"><div><div className="font-semibold text-slate-900">{variation.name}</div><div className="text-xs text-slate-500">{variation.dimension_label}</div></div><span className="text-xs font-semibold text-slate-500">Edit</span></summary><div className="border-t border-slate-200 p-4"><VariationForm variation={variation} families={families}/></div></details>)}</div></Section>
    <Section title="Material & Process Cost Master" description="All source Master fields are editable. Blank rate means Needs rate; it is never treated as zero." count={costs.length}><details className="mb-3 rounded-xl border border-yellow-200 bg-yellow-50/40 p-3"><summary className="cursor-pointer text-sm font-bold text-slate-800">+ Add Material / Process</summary><div className="mt-3"><CostForm families={families} selected={new Set()}/></div></details><div className="space-y-2">{costs.map((item:any)=><details key={item.id} className="rounded-xl border border-slate-200"><summary className="flex cursor-pointer items-center justify-between gap-3 px-4 py-3"><div><div className="font-semibold text-slate-900">{item.name}</div><div className="text-xs text-slate-500">{item.code} · {item.current_rate==null?'Needs rate':`${item.currency} ${item.current_rate}/${item.rate_uom}`}</div></div><span className="text-xs font-semibold text-slate-500">Edit</span></summary><div className="border-t border-slate-200 p-4"><CostForm item={item} families={families} selected={costFamilies(item.id)}/></div></details>)}</div></Section>
    <Section title="Extras · Pre · Post Charges" description="Charge rate, basis, application stage and family availability are editable source values." count={charges.length}><details className="mb-3 rounded-xl border border-yellow-200 bg-yellow-50/40 p-3"><summary className="cursor-pointer text-sm font-bold text-slate-800">+ Add Charge</summary><div className="mt-3"><ChargeForm families={families} selected={new Set()}/></div></details><div className="space-y-2">{charges.map((item:any)=><details key={item.id} className="rounded-xl border border-slate-200"><summary className="flex cursor-pointer items-center justify-between gap-3 px-4 py-3"><div><div className="font-semibold text-slate-900">{item.name}</div><div className="text-xs text-slate-500">{item.category} · {item.current_rate==null?'Needs rate':`${item.currency} ${item.current_rate}`}</div></div><span className="text-xs font-semibold text-slate-500">Edit</span></summary><div className="border-t border-slate-200 p-4"><ChargeForm item={item} families={families} selected={chargeFamilies(item.id)}/></div></details>)}</div></Section>
  </div>;
}
