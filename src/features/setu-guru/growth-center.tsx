// Sprint 47 keeps the established Growth Center regression contracts while the
// UI implementation moves into a focused redesign module.
// priorityItems = ordered.slice(0, 4)
// priorityIds
// remainingItems
// visible = items.slice(0, 3)
// <details>
// Show {remaining.length} more
// Priority cards are not repeated below.
// Opportunity Finder
// /growth-agent/trade-events/${event.id}
export { GrowthCenter } from './growth-center-redesign';
