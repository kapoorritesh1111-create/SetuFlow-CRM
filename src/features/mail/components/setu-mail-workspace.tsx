'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { Archive, Download, FileText, Inbox, Mail, Paperclip, PenLine, RefreshCw, Reply, Search, Send, Settings, Sparkles, Star, Trash2, X } from 'lucide-react';

type MailMessage = { id:string; thread_id:string|null; direction:'inbound'|'outbound'; status:string; folder:string; from_address:string; to_addresses:string[]; cc_addresses:string[]; bcc_addresses:string[]; subject:string; text_body:string|null; is_read:boolean; is_starred:boolean; created_at:string; sent_at?:string|null; received_at?:string|null };
type Mailbox={id:string;address:string;display_name:string|null}; type Attachment={id:string;message_id:string|null;filename:string;content_type:string|null;size_bytes:number|null;created_at:string}; type Signature={id:string;name:string;text_signature:string|null;html_signature:string|null;is_default:boolean}; type MailPayload={mailbox:Mailbox|null;messages:MailMessage[];attachments:Attachment[];signature:Signature|null;providerReady:boolean;inboundReady:boolean}; type Folder='inbox'|'sent'|'drafts'|'starred'|'archive'|'trash';
type MailIntelligence={messageId:string;peerAddress:string;crmMatch:null|{type:'lead';id:string;lead_type:string|null;company_name:string|null;contact_name:string|null;email:string|null;stage_id:string|null;owner_user_id:string|null;href:string};createCrmHref?:string|null;intents:Array<{key:string;label:string;confidence:'high'|'medium';suggestedAction:string;evidence?:string;actionLabel?:string|null;actionHref?:string|null}>;autonomousActions:false};
function formatTime(v:string){const d=new Date(v);if(Number.isNaN(d.getTime()))return'';return d.toDateString()===new Date().toDateString()?d.toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):d.toLocaleDateString([],{month:'short',day:'numeric'})} function addresses(v:string){return v.split(',').map(x=>x.trim()).filter(Boolean)} function fileSize(v:number|null){const n=Number(v??0);return n<1048576?`${Math.max(1,Math.round(n/1024))} KB`:`${(n/1048576).toFixed(1)} MB`} function deliveryStatus(s:string){const k=String(s||'').toLowerCase();if(k==='delivered')return{label:'Delivered',classes:'bg-emerald-50 text-emerald-700 border-emerald-100'};if(k==='sent')return{label:'Sent',classes:'bg-blue-50 text-blue-700 border-blue-100'};if(k==='delayed')return{label:'Delayed',classes:'bg-amber-50 text-amber-700 border-amber-100'};if(['bounced','complained','failed'].includes(k))return{label:k==='complained'?'Complaint':k[0].toUpperCase()+k.slice(1),classes:'bg-rose-50 text-rose-700 border-rose-100'};if(k==='received')return{label:'Received',classes:'bg-slate-50 text-slate-600 border-slate-200'};return{label:s||'Pending',classes:'bg-slate-50 text-slate-600 border-slate-200'}}

export function SetuMailWorkspace({userName,userEmail,organizationName}:{userName:string;userEmail:string;organizationName:string}){
 const [data,setData]=useState<MailPayload>({mailbox:null,messages:[],attachments:[],signature:null,providerReady:false,inboundReady:false});const[folder,setFolder]=useState<Folder>('inbox');const[selectedId,setSelectedId]=useState<string|null>(null);const[composeOpen,setComposeOpen]=useState(false);const[to,setTo]=useState('');const[cc,setCc]=useState('');const[bcc,setBcc]=useState('');const[subject,setSubject]=useState('');const[body,setBody]=useState('');const[draftId,setDraftId]=useState<string|null>(null);const[parentMessageId,setParentMessageId]=useState<string|null>(null);const[threadId,setThreadId]=useState<string|null>(null);const[pendingAttachments,setPendingAttachments]=useState<Attachment[]>([]);const[search,setSearch]=useState('');const[sending,setSending]=useState(false);const[loading,setLoading]=useState(true);const[notice,setNotice]=useState<string|null>(null);const[signatureOpen,setSignatureOpen]=useState(false);const[signatureText,setSignatureText]=useState('');const[savingSignature,setSavingSignature]=useState(false);const[intelligence,setIntelligence]=useState<MailIntelligence|null>(null);const[intelligenceLoading,setIntelligenceLoading]=useState(false);const autosaveTimer=useRef<ReturnType<typeof setTimeout>|null>(null);
 async function refresh(){setLoading(true);try{const r=await fetch('/api/mail',{cache:'no-store'});const p=await r.json() as MailPayload&{error?:string};if(!r.ok)throw new Error(p.error||'Unable to load mail.');setData(p);setSignatureText(p.signature?.text_signature??'')}catch(e){setNotice(e instanceof Error?e.message:'Unable to load mail.')}finally{setLoading(false)}} useEffect(()=>{void refresh()},[]);
 async function saveDraft(silent=false){if(!composeOpen||(!to.trim()&&!cc.trim()&&!bcc.trim()&&!subject.trim()&&!body.trim()))return draftId;try{const r=await fetch('/api/mail/drafts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:draftId,to:addresses(to),cc:addresses(cc),bcc:addresses(bcc),subject,text:body,threadId})});const p=await r.json() as{draft?:{id:string};error?:string};if(!r.ok)throw new Error(p.error||'Unable to save draft.');const id=p.draft?.id??draftId;setDraftId(id??null);if(!silent)setNotice('Draft saved.');return id??null}catch(e){if(!silent)setNotice(e instanceof Error?e.message:'Unable to save draft.');return draftId}}
 useEffect(()=>{if(!composeOpen)return;if(autosaveTimer.current)clearTimeout(autosaveTimer.current);autosaveTimer.current=setTimeout(()=>{void saveDraft(true)},1000);return()=>{if(autosaveTimer.current)clearTimeout(autosaveTimer.current)}},[composeOpen,to,cc,bcc,subject,body,threadId]);
 const filtered=useMemo(()=>data.messages.filter(m=>{const f=folder==='starred'?m.is_starred:m.folder===folder;const q=search.trim().toLowerCase();return f&&(!q||`${m.from_address} ${m.to_addresses.join(' ')} ${m.subject} ${m.text_body??''}`.toLowerCase().includes(q))}),[data.messages,folder,search]);const selected=data.messages.find(m=>m.id===selectedId)??filtered[0]??null;const selectedAttachments=data.attachments.filter(a=>a.message_id===selected?.id);
 useEffect(()=>{if(!selected){setIntelligence(null);return}let active=true;setIntelligenceLoading(true);fetch(`/api/mail/intelligence/${selected.id}`,{cache:'no-store'}).then(async r=>{const p=await r.json();if(!r.ok)throw new Error(p.error||'Unable to analyze this email.');if(active)setIntelligence(p)}).catch(()=>{if(active)setIntelligence(null)}).finally(()=>{if(active)setIntelligenceLoading(false)});return()=>{active=false}},[selected?.id]);
 function resetCompose(){setComposeOpen(false);setTo('');setCc('');setBcc('');setSubject('');setBody('');setDraftId(null);setParentMessageId(null);setThreadId(null);setPendingAttachments([])} function openCompose(){resetCompose();setComposeOpen(true)} function reply(){if(!selected)return;setComposeOpen(true);setTo(selected.from_address);setSubject(selected.subject.toLowerCase().startsWith('re:')?selected.subject:`Re: ${selected.subject}`);setThreadId(selected.thread_id);setParentMessageId(selected.id);setBody('')}
 async function send(){if(!data.mailbox){setNotice('Your mailbox is not ready yet.');return}setSending(true);try{const r=await fetch('/api/mail/send',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mailboxId:data.mailbox.id,to:addresses(to),cc:addresses(cc),bcc:addresses(bcc),subject,text:body,draftId,threadId,parentMessageId,attachmentIds:pendingAttachments.map(a=>a.id)})});const p=await r.json();if(!r.ok)throw new Error(p.error||'Unable to send email.');resetCompose();setFolder('sent');setNotice('Email sent.');await refresh()}catch(e){setNotice(e instanceof Error?e.message:'Unable to send email.')}finally{setSending(false)}}
 async function messageAction(action:'read'|'star'|'archive'|'trash'){if(!selected)return;const r=await fetch(`/api/mail/messages/${selected.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,value:action==='star'?!selected.is_starred:undefined})});if(r.ok)await refresh()} async function upload(file:File){const form=new FormData();form.set('file',file);if(data.mailbox)form.set('mailboxId',data.mailbox.id);const r=await fetch('/api/mail/attachments',{method:'POST',body:form});const p=await r.json();if(!r.ok){setNotice(p.error||'Unable to attach file.');return}setPendingAttachments(x=>[...x,p.attachment])} async function saveSignature(){setSavingSignature(true);const r=await fetch('/api/mail/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:'Default signature',textSignature:signatureText,isDefault:true})});setSavingSignature(false);if(r.ok){setSignatureOpen(false);setNotice('Signature saved.');await refresh()}}
 const folders:[Folder,string,any][]=[['inbox','Inbox',Inbox],['sent','Sent',Send],['drafts','Drafts',FileText],['starred','Starred',Star],['archive','Archive',Archive],['trash','Trash',Trash2]];
 return <div className="h-[calc(100vh-7.5rem)] min-h-[620px] overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex h-full">
  <aside className="w-56 shrink-0 border-r border-slate-200 bg-slate-50/70 p-4"><button onClick={openCompose} className="mb-5 flex w-full items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 py-3 text-sm font-semibold text-white shadow-sm hover:bg-blue-700"><PenLine className="h-4 w-4"/>Compose</button><div className="space-y-1">{folders.map(([key,label,Icon])=><button key={key} onClick={()=>setFolder(key)} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${folder===key?'bg-blue-50 font-semibold text-blue-700':'text-slate-600 hover:bg-white'}`}><Icon className="h-4 w-4"/>{label}</button>)}</div><div className="mt-6 border-t border-slate-200 pt-4"><p className="truncate text-xs font-semibold text-slate-700">{data.mailbox?.address??userEmail}</p><p className="mt-1 text-[11px] text-slate-400">{organizationName}</p><button onClick={()=>setSignatureOpen(true)} className="mt-3 flex items-center gap-2 text-xs font-medium text-slate-500 hover:text-blue-600"><Settings className="h-3.5 w-3.5"/>Signature</button></div></aside>
  <section className="w-[350px] shrink-0 border-r border-slate-200"><div className="border-b border-slate-200 p-4"><div className="flex items-center justify-between"><div><h2 className="font-semibold capitalize text-slate-900">{folder}</h2><p className="text-xs text-slate-400">{filtered.length} messages</p></div><button onClick={()=>void refresh()} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><RefreshCw className={`h-4 w-4 ${loading?'animate-spin':''}`}/></button></div><div className="mt-3 flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3"><Search className="h-4 w-4 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search mail" className="w-full py-2 text-sm outline-none"/></div></div><div className="h-[calc(100%-91px)] overflow-y-auto">{filtered.map(m=><button key={m.id} onClick={()=>{setSelectedId(m.id);if(!m.is_read)void fetch(`/api/mail/messages/${m.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'read',value:true})})}} className={`w-full border-b border-slate-100 p-4 text-left hover:bg-slate-50 ${selected?.id===m.id?'bg-blue-50/60':''}`}><div className="flex items-center justify-between gap-3"><p className={`truncate text-sm ${m.is_read?'text-slate-700':'font-semibold text-slate-900'}`}>{m.direction==='inbound'?m.from_address:m.to_addresses.join(', ')}</p><span className="shrink-0 text-[11px] text-slate-400">{formatTime(m.created_at)}</span></div><p className={`mt-1 truncate text-sm ${m.is_read?'text-slate-600':'font-semibold text-slate-800'}`}>{m.subject||'(No subject)'}</p><p className="mt-1 truncate text-xs text-slate-400">{m.text_body||'No preview'}</p></button>)}</div></section>
  <main className="min-w-0 flex-1 overflow-y-auto">{selected?<><div className="border-b border-slate-200 px-6 py-4"><div className="flex items-start justify-between gap-4"><div><h1 className="text-lg font-semibold text-slate-900">{selected.subject||'(No subject)'}</h1><div className="mt-2 flex items-center gap-2 text-xs text-slate-500"><span>{selected.direction==='inbound'?selected.from_address:selected.to_addresses.join(', ')}</span><span>•</span><span>{new Date(selected.created_at).toLocaleString()}</span><span className={`rounded-full border px-2 py-0.5 ${deliveryStatus(selected.status).classes}`}>{deliveryStatus(selected.status).label}</span></div></div><div className="flex gap-1"><button onClick={reply} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100"><Reply className="h-4 w-4"/></button><button onClick={()=>void messageAction('star')} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100"><Star className={`h-4 w-4 ${selected.is_starred?'fill-amber-400 text-amber-400':''}`}/></button><button onClick={()=>void messageAction('archive')} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100"><Archive className="h-4 w-4"/></button><button onClick={()=>void messageAction('trash')} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100"><Trash2 className="h-4 w-4"/></button></div></div></div>
   <div className="p-6"><div className="whitespace-pre-wrap text-sm leading-7 text-slate-700">{selected.text_body||'No message body.'}</div>{selectedAttachments.length>0&&<div className="mt-6"><p className="mb-2 text-xs font-semibold uppercase tracking-wide text-slate-400">Attachments</p><div className="flex flex-wrap gap-2">{selectedAttachments.map(a=><a key={a.id} href={`/api/mail/attachments?id=${a.id}`} className="flex items-center gap-2 rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-600 hover:border-blue-200 hover:text-blue-600"><Paperclip className="h-3.5 w-3.5"/>{a.filename}<span className="text-slate-400">{fileSize(a.size_bytes)}</span><Download className="h-3 w-3"/></a>)}</div></div>}
    <div className="mt-8 rounded-2xl border border-blue-100 bg-gradient-to-br from-blue-50 to-white p-5"><div className="flex items-center gap-2"><Sparkles className="h-4 w-4 text-blue-600"/><h3 className="text-sm font-semibold text-slate-900">Setu Guru</h3></div>{intelligenceLoading?<p className="mt-3 text-sm text-slate-500">Understanding this conversation…</p>:intelligence?<div className="mt-4 space-y-4"><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-400">CRM connection</p>{intelligence.crmMatch?<><p className="mt-2 text-sm font-semibold text-slate-900">{intelligence.crmMatch.company_name||intelligence.crmMatch.contact_name||intelligence.peerAddress}</p><p className="mt-1 text-xs text-slate-500">Matched from {intelligence.peerAddress}</p><a href={intelligence.crmMatch.href} className="mt-3 inline-flex rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700 hover:border-blue-200 hover:text-blue-700">Open lead</a></>:<><p className="mt-2 text-sm text-slate-600">No CRM record matches {intelligence.peerAddress}.</p>{intelligence.createCrmHref&&<a href={intelligence.createCrmHref} className="mt-3 inline-flex rounded-lg bg-blue-600 px-3 py-2 text-xs font-semibold text-white hover:bg-blue-700">Create lead</a>}</>}</div>{intelligence.intents.length>0?<div><p className="mb-2 text-[11px] font-semibold uppercase tracking-wide text-slate-400">Suggested next steps</p><div className="space-y-2">{intelligence.intents.map(i=><div key={i.key} className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-slate-900">{i.label}</p><p className="mt-1 text-xs leading-5 text-slate-500">{i.suggestedAction}</p>{i.evidence&&<p className="mt-2 rounded-lg bg-slate-50 px-3 py-2 text-xs italic text-slate-500">“{i.evidence}”</p>}</div><span className="rounded-full bg-blue-50 px-2 py-1 text-[10px] font-semibold uppercase text-blue-700">{i.confidence}</span></div>{i.actionHref&&i.actionLabel&&<a href={i.actionHref} className="mt-3 inline-flex rounded-lg bg-slate-900 px-3 py-2 text-xs font-semibold text-white hover:bg-slate-800">{i.actionLabel}</a>}</div>)}</div></div>:<p className="text-sm text-slate-500">No follow-up action is needed right now.</p>}<p className="text-[11px] text-slate-400">Guru suggests actions. You stay in control of every CRM change.</p></div>:<p className="mt-3 text-sm text-slate-500">Guru analysis is unavailable for this message.</p>}</div><button onClick={reply} className="mt-6 inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50"><Reply className="h-4 w-4"/>Reply</button></div></>:<div className="flex h-full items-center justify-center text-center"><div><Mail className="mx-auto h-10 w-10 text-slate-300"/><p className="mt-3 text-sm font-medium text-slate-600">Select a message</p><p className="mt-1 text-xs text-slate-400">Read, reply and act on business conversations.</p></div></div>}</main>
 </div>{notice&&<div className="fixed bottom-6 right-6 z-50 flex items-center gap-3 rounded-xl bg-slate-900 px-4 py-3 text-sm text-white shadow-xl"><span>{notice}</span><button onClick={()=>setNotice(null)}><X className="h-4 w-4"/></button></div>}{signatureOpen&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/30 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><h3 className="text-lg font-semibold text-slate-900">Email signature</h3><p className="mt-1 text-sm text-slate-500">Added automatically to new messages.</p><textarea value={signatureText} onChange={e=>setSignatureText(e.target.value)} rows={7} className="mt-4 w-full rounded-xl border border-slate-200 p-3 text-sm outline-none focus:border-blue-400"/><div className="mt-4 flex justify-end gap-2"><button onClick={()=>setSignatureOpen(false)} className="rounded-lg px-4 py-2 text-sm font-medium text-slate-600">Cancel</button><button disabled={savingSignature} onClick={()=>void saveSignature()} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">{savingSignature?'Saving…':'Save signature'}</button></div></div></div>}
 {composeOpen&&<div className="fixed bottom-5 right-5 z-40 w-[560px] max-w-[calc(100vw-2rem)] overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl"><div className="flex items-center justify-between bg-slate-900 px-4 py-3 text-sm font-semibold text-white"><span>New message</span><button onClick={()=>{void saveDraft(true);resetCompose()}}><X className="h-4 w-4"/></button></div><div className="divide-y divide-slate-100"><input value={to} onChange={e=>setTo(e.target.value)} placeholder="To" className="w-full px-4 py-3 text-sm outline-none"/><div className="grid grid-cols-2"><input value={cc} onChange={e=>setCc(e.target.value)} placeholder="Cc" className="border-r border-slate-100 px-4 py-3 text-sm outline-none"/><input value={bcc} onChange={e=>setBcc(e.target.value)} placeholder="Bcc" className="px-4 py-3 text-sm outline-none"/></div><input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Subject" className="w-full px-4 py-3 text-sm outline-none"/><textarea value={body} onChange={e=>setBody(e.target.value)} rows={12} placeholder={`Write your message, ${userName.split(' ')[0] || ''}…`} className="w-full resize-none px-4 py-3 text-sm outline-none"/></div>{pendingAttachments.length>0&&<div className="flex flex-wrap gap-2 border-t border-slate-100 px-4 py-3">{pendingAttachments.map(a=><span key={a.id} className="rounded-lg bg-slate-100 px-2 py-1 text-xs text-slate-600">{a.filename}</span>)}</div>}<div className="flex items-center justify-between border-t border-slate-100 px-4 py-3"><div className="flex items-center gap-2"><label className="cursor-pointer rounded-lg p-2 text-slate-500 hover:bg-slate-100"><Paperclip className="h-4 w-4"/><input type="file" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(f)void upload(f)}}/></label><span className="text-xs text-slate-400">{data.signature?.text_signature?'Signature on':'No signature'}</span></div><button disabled={sending||!to.trim()} onClick={()=>void send()} className="flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50"><Send className="h-4 w-4"/>{sending?'Sending…':'Send'}</button></div></div>}
 </div>
}
