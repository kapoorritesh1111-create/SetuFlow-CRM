export type VCardIdentity = { fullName: string; email: string; organizationName: string; roleLabel?: string | null; username?: string | null; previewPath?: string | null; };
function escapeVCardValue(value: string) { return value.replace(/\\/g, '\\\\').replace(/\n/g, '\\n').replace(/,/g, '\\,').replace(/;/g, '\\;'); }
function slugifyFilePart(value: string) { return value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'setu-flow-contact'; }
export function buildVCard(identity: VCardIdentity) { const lines = ['BEGIN:VCARD','VERSION:3.0',`FN:${escapeVCardValue(identity.fullName)}`,`ORG:${escapeVCardValue(identity.organizationName)}`,`EMAIL;TYPE=INTERNET:${escapeVCardValue(identity.email)}`]; if (identity.roleLabel?.trim()) lines.push(`TITLE:${escapeVCardValue(identity.roleLabel.trim())}`); if (identity.username?.trim()) lines.push(`NICKNAME:${escapeVCardValue(identity.username.trim())}`); if (identity.previewPath?.trim()) lines.push(`URL:${escapeVCardValue(identity.previewPath.trim())}`); lines.push('NOTE:Generated by SETU Flow Global Contact Exchange'); lines.push('END:VCARD'); return `${lines.join('\r\n')}\r\n`; }
export function getVCardFilename(identity: Pick<VCardIdentity, 'fullName'>) { return `${slugifyFilePart(identity.fullName)}-setu-flow-contact.vcf`; }
