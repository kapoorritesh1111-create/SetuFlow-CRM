import Link from 'next/link';
function messageFor(notice?: string) { if (notice === 'service-role-missing') return 'Your form was accepted in preview mode. Configure the Supabase service role key to store public onboarding requests automatically.'; if (notice === 'storage-pending') return 'Your form generated the workspace request, but the database table is not available yet. Apply the onboarding migration before production intake.'; if (notice === 'submitted-notified') return 'Your onboarding request was received and admin@setugroups.com has been notified with a direct setup link.'; if (notice === 'submitted-notification-pending') return 'Your onboarding request was received. The admin setup link was recorded, but outbound email notification still needs email-provider environment variables.'; return 'Your onboarding request was received. The Setu Flow team can now complete setup and send the first admin invite.'; }
export default function OnboardingReceivedPage({ searchParams }: { searchParams?: { company?: string; domain?: string; notice?: string } }) { return <main className="min-h-screen bg-slate-50 px-4 py-12 text-slate-900"><section className="mx-auto max-w-3xl rounded-hero border border-slate-200 bg-white p-8 text-center shadow-[0_24px_80px_rgba(15,23,42,0.08)]"><p className="text-xs font-extrabold uppercase tracking-[0.22em] text-emerald-600">Onboarding request</p><h1 className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">Workspace setup queued</h1><p className="mt-4 text-base leading-7 text-slate-600">{messageFor(searchParams?.notice)}</p><div className="mt-6 rounded-3xl border border-blue-100 bg-blue-50 p-5 text-left"><p className="text-sm font-bold text-slate-950">Company</p><p className="mt-1 text-sm text-slate-700">{searchParams?.company ?? 'New client'}</p><p className="mt-4 text-sm font-bold text-slate-950">Reserved workspace domain</p><p className="mt-1 text-sm font-semibold text-blue-800">{searchParams?.domain ?? 'companyname.setuflowcrm.com'}</p></div><div className="mt-7 flex flex-wrap justify-center gap-3"><Link href="/onboarding" className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-50">Submit another request</Link><Link href="/client-login" className="rounded-2xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white hover:bg-slate-800">Client login</Link></div></section></main>; }
