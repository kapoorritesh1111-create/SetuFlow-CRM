import {NextRequest,NextResponse} from 'next/server';import {getCurrentWorkspace} from '@/lib/workspace/auth';export const dynamic='force-dynamic';
export async function GET(req:NextRequest){const w=await getCurrentWorkspace();if(!w.user||!w.organization||!w.membership)return NextResponse.redirect(new URL('/login',req.url));const clientId=process.env.ZOOM_CLIENT_ID;if(!clientId)return NextResponse.json({error:'Zoom connection is not configured.'},{status:503});const state=Buffer.from(JSON.stringify({u:w.user.id,o:w.organization.id,t:Date.now()})).toString('base64url');const redirectUri=`${req.nextUrl.origin}/api/calendar/zoom/callback`;const url=new URL('https://zoom.us/oauth/authorize');url.searchParams.set('response_type','code');url.searchParams.set('client_id',clientId);url.searchParams.set('redirect_uri',redirectUri);url.searchParams.set('state',state);return NextResponse.redirect(url);}
