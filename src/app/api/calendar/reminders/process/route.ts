import {NextRequest,NextResponse}from'next/server';import{createServiceRoleClient}from'@/lib/supabase/service-role';export const dynamic='force-dynamic';
function esc(v:unknown){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]||c))}
export async function GET(req:NextRequest){const secret=String(process.env.CRON_SECRET||'').trim();if(!secret||req.headers.get('authorization')!==`Bearer ${secret}`)return NextResponse.json({error:'Unauthorized'},{status:401});const db=createServiceRoleClient();if(!db)return NextResponse.json({error:'Service unavailable'},{status:503});const now=new Date(),lookback=new Date(now.getTime()-24*60*60*1000);const{data:reminders}=await db.from('calendar_reminders').select('*,calendar_events(*)').is('sent_at',null).gte('created_at',lookback.toISOString()).limit(200);let processed=0,failed=0;for(const r of reminders??[]){const e:any=r.calendar_events;if(!e||e.status==='cancelled')continue;const due=new Date(new Date(e.starts_at).getTime()-r.minutes_before*60000);if(due>now)continue;if(new Date(e.starts_at)<lookback){await db.from('calendar_reminders').update({sent_at:now.toISOString()}).eq('id',r.id);continue}try{if(r.channel==='email'){if(!process.env.RESEND_API_KEY||!process.env.RESEND_FROM_EMAIL)throw new Error('Email reminders are not configured');const{data:profile}=await db.from('profiles').select('email').eq('id',e.owner_user_id).maybeSingle();if(profile?.email){const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.RESEND_FROM_EMAIL,to:[profile.email],subject:`Reminder: ${e.title}`,html:`<div style="font-family:Arial,sans-serif;max-width:560px"><h2>${esc(e.title)}</h2><p>Starts ${esc(new Intl.DateTimeFormat('en',{dateStyle:'medium',timeStyle:'short',timeZone:e.timezone||'UTC'}).format(new Date(e.starts_at)))} (${esc(e.timezone||'UTC')})</p>${e.meeting_url?`<p><a href="${esc(e.meeting_url)}" style="display:inline-block;padding:12px 18px;background:#0f172a;color:#fff;text-decoration:none;border-radius:10px">Join meeting</a></p>`:''}</div>`})});if(!response.ok)throw new Error('Reminder email failed')}}await db.from('calendar_reminders').update({sent_at:now.toISOString()}).eq('id',r.id);processed++}catch{failed++}}return NextResponse.json({ok:true,processed,failed})}
