import { NextResponse } from 'next/server';
import { getCurrentWorkspace, getWorkspaceRoleNames } from '@/lib/workspace/auth';
import { getPrimaryWorkspaceRole, getWorkspaceRoleDisplayName } from '@/lib/workspace/roles';
import { buildVCard, getVCardFilename } from '@/lib/contact-exchange/vcard';
export async function GET() { const workspace = await getCurrentWorkspace(); if (!workspace.user) return NextResponse.json({ error: 'Authentication required.' }, { status: 401 }); if (!workspace.membership || !workspace.organization) return NextResponse.json({ error: 'Active workspace membership required.' }, { status: 403 }); const currentRoles = await getWorkspaceRoleNames(workspace.membership.id); const primaryRole = getPrimaryWorkspaceRole(currentRoles) || 'member'; const roleLabel = getWorkspaceRoleDisplayName(primaryRole); const fullName = workspace.profile?.full_name?.trim() || workspace.user.email?.split('@')[0] || 'SETU Flow user'; const email = workspace.profile?.email || workspace.user.email || 'email-not-available@setu.flow'; const username = workspace.profile?.username || null; const vcard = buildVCard({ fullName, email, organizationName: workspace.organization.name, roleLabel, username, previewPath: '/card' }); return new NextResponse(vcard, { status: 200, headers: { 'Content-Type': 'text/vcard; charset=utf-8', 'Content-Disposition': `attachment; filename="${getVCardFilename({ fullName })}"`, 'Cache-Control': 'private, no-store', }, }); }
