import { RouteLoadingState } from '@/components/app/route-loading-state';

export default function Loading() {
  return <RouteLoadingState eyebrow="Orders / Execution" title="Loading execution-readiness desk" description="Preparing commercial lock truth, release blockers, documentary posture, and dispatch evidence for the active workspace." />;
}
