import { RouteLoadingState } from '@/components/app/route-loading-state';
export default function Loading(){return <RouteLoadingState eyebrow="Admin" title="Loading product management" description="Preparing the admin-owned product system of record, catalog pricing coverage, and downstream workflow links." />;}
