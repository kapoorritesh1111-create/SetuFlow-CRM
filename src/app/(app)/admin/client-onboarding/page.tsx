import Link from 'next/link';
import { SectionCard } from '@/components/ui/section-card';
import { StateMessage } from '@/components/ui/state-message';
import { StatusBadge } from '@/components/ui/status-badge';
import { AdminPageHero, AdminSettingsShell, type AdminGapItem } from '@/features/admin/components/admin-settings-shell';
import { createWorkspaceFromOnboardingDraft, updateClientOnboardingStatus } from '@/features/client-onboarding/server/actions';
import { hasSupabaseEnv } from '@/lib/env';
import { createClient } from '@/lib/supabase/server';
import { requireAdminWorkspace } from '@/lib/workspace/auth';

type OnboardingRequest = { id: string; company_name: string; company_slug: string | null; workspace_domain: string | null; logo_url: string | null; website: string | null; primary_admin_name: string | null; primary_admin_email: string | null; headquarters_country: string | null; requested_markets: string[] | null; requested_countries: string[] | null; requested_pipelines: string[] | null; requested_pipeline_stages: string[] | null; requested_next_steps: string[] | null; pricing_rules_notes: string | null; product_category_notes: string | null; wants_trade_events: boolean | null; status: string; linked_organization_id: string | null; created_at: string; updated_at: string; };
const DEFAULT_LOGO = '/logos/setu-flow-logo.png';
const fallbackStages = ['New lead', 'Qualified', 'Samples / documents', 'Quote sent', 'Negotiation', 'Won', 'Lost'];
const fallbackPipelines = ['Buyer pipeline', 'Supplier pipeline'];
const fallbackNextSteps = ['Call back', 'Send catalog', 'Send quote', 'Share sample details', 'Follow up after trade show', 'Schedule meeting'];
const fallbackMarkets = ['North America', 'Middle East', 'Europe', 'Asia Pacific'];
const statusOptions = ['submitted', 'reviewing', 'setup_in_progress', 'admin_invite_ready', 'admin_invited', 'live', 'paused'];
function toneForStatus(status: string): 'success' | 'warning' | 'info' | 'neutral' | 'danger' { if (status === 'live') return 'success'; if (status === 'paused') return 'danger'; if (status.includes('progress') || status.includes('ready')) return 'info'; if (status === 'submitted') return 'warning'; return 'neutral'; }
function formatStatus(status: string) { return status.split('_').map((part) => part.charAt(0).toUpperCase() + part.slice(1)).join(' '); }
function listValue(value: string[] | null | undefined, fallback: string[]) { return (Array.isArray(value) && value.length > 0 ? value : fallback).slice(0, 7); }
function ChecklistItem({ done, label, href }: { done: boolean; label: string; href?: string }) { const content = <span className={(done ? 'border-emerald-200 bg-emerald-50 text-emerald-800' : 'border-amber-200 bg-amber-50 text-amber-900') + ' inline-flex min-h-10 items-center rounded-2xl border px-3 py-2 text-sm font-semibold'}>{done ? '✓' : '!'} {label}</span>; return href ? <Link href={href}>{content}</Link> : content; }
function AdminStep({ number, title, description, href }: { number: string; title: string; description: string; href?: string }) { const body = <div className="h-full rounded-3xl border border-slate-200 bg-white p-5 shadow-[0_18px_45px_rgba(15,23,42,0.06)] transition hover:-translate-y-0.5 hover:border-blue-200"><span className="inline-flex h-9 w-9 items-center justify-center rounded-2xl bg-blue-50 text-sm font-extrabold text-blue-700">{number}</span><h3 className="mt-4 text-lg font-semibold tracking-tight text-slate-950">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{description}</p></div>; return href ? <Link href={href}>{body}</Link> : body; }
function ListBlock({ title, items }: { title: string; items: string[] }) { return <div className="rounded-3xl border border-slate-200 bg-slate-50 p-4"><p className="text-sm font-bold text-slate-950">{title}</p><div className="mt-3 flex flex-wrap gap-2">{items.map((item) => <span key={item} className="rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-semibold text-slate-700">{item}</span>)}</div></div>; }
function RequestCard({ request }: { request: OnboardingRequest }) { const logo = request.logo_url || DEFAULT_LOGO; return <article className="rounded-[2rem] border border-slate-200 bg-white p-5 shadow-[0_22px_60px_rgba(15,23,42,0.07)]"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div className="flex gap-4"><img src={logo} alt="Client logo" width={56} height={56} className="h-14 w-14 rounded-2xl border border-slate-200 bg-white object-contain p-1" /><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-semibold tracking-tight text-slate-950">{request.company_name}</h2><StatusBadge label={formatStatus(request.status)} tone={toneForStatus(request.status)} dot={false} /></div><p className="mt-1 text-sm text-slate-600">{request.workspace_domain ?? `${request.company_slug ?? 'companyname'}.setuflowcrm.com`}</p><p className="mt-1 text-sm text-slate-500">Admin: {request.primary_admin_name ?? 'Name pending'} · {request.primary_admin_email ?? 'Email pending'}</p></div></div><div className="flex flex-wrap gap-2">{request.website ? <Link href={request.website} className="rounded-2xl border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700 hover:bg-slate-50">Website</Link> : null}<Link href="/admin/invitations" className="rounded-2xl bg-slate-950 px-3 py-2 text-xs font-semibold text-white hover:bg-slate-800">Send admin invite</Link></div></div><div className="mt-5 grid gap-4 xl:grid-cols-2"><ListBlock title="Markets" items={listValue(request.requested_markets, fallbackMarkets)} /><ListBlock title="Countries" items={listValue(request.requested_countries, request.headquarters_country ? [request.headquarters_country] : ['Add countries during setup'])} /><ListBlock title="Pipelines" items={listValue(request.requested_pipelines, fallbackPipelines)} /><ListBlock title="Pipeline stages" items={listValue(request.requested_pipeline_stages, fallbackStages)} /><ListBlock title="Next steps" items={listValue(request.requested_next_steps, fallbackNextSteps)} /><div className="rounded-3xl border border-slate-200 bg-slate-50 p-4"><p className="text-sm font-bold text-slate-950">Client-created product categories</p><p className="mt-2 text-sm leading-6 text-slate-600">{request.product_category_notes || 'No product categories are pre-created. Client creates their own categories after first login.'}</p><p className="mt-3 text-sm font-bold text-slate-950">Pricing rule notes</p><p className="mt-2 text-sm leading-6 text-slate-600">{request.pricing_rules_notes || 'Pricing rules to be configured per client.'}</p></div></div><div className="mt-5 flex flex-col gap-3 border-t border-slate-200 pt-5 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-wrap gap-2"><ChecklistItem done={Boolean(request.logo_url)} label={request.logo_url ? 'Client logo provided' : 'Setu Flow logo fallback'} /><ChecklistItem done={Boolean(request.linked_organization_id)} label="Workspace drafted" /><ChecklistItem done={request.status === 'admin_invited' || request.status === 'live'} label="First admin invite sent" href="/admin/invitations" /><ChecklistItem done={Boolean(request.wants_trade_events)} label={request.wants_trade_events ? 'Trade events requested' : 'Trade events optional'} href="/admin/trade-events" /></div><div className="flex flex-wrap gap-2">{!request.linked_organization_id ? <form action={createWorkspaceFromOnboardingDraft}><input type="hidden" name="request_id" value={request.id} /><input type="hidden" name="company_name" value={request.company_name} /><input type="hidden" name="logo_url" value={logo} /><button className="rounded-2xl border border-blue-200 bg-blue-50 px-3 py-2 text-xs font-bold text-blue-800 hover:bg-blue-100">Draft workspace</button></form> : null}<form action={updateClientOnboardingStatus} className="flex gap-2"><input type="hidden" name="request_id" value={request.id} /><select name="status" defaultValue={request.status} className="min-h-9 rounded-2xl border border-slate-200 bg-white px-3 py-1 text-xs font-semibold text-slate-700">{statusOptions.map((status) => <option key={status} value={status}>{formatStatus(status)}</option>)}</select><button className="rounded-2xl bg-slate-950 px-3 py-2 text-xs font-bold text-white hover:bg-slate-800">Update</button></form></div></div></article>; }

export default async function ClientOnboardingAdminPage({ searchParams }: { searchParams?: { notice?: string } }) {
  if (!hasSupabaseEnv) return <StateMessage title="Supabase environment variables are missing" description="Configure the application environment before using client onboarding." tone="warning" />;
  const { missingEnv, organization } = await requireAdminWorkspace();
  if (missingEnv) return <StateMessage title="Supabase environment variables are missing" description="Configure the application environment before using client onboarding." tone="warning" />;
  if (!organization) return null;
  const supabase = (await createClient()) as any;
  const { data, error } = await supabase.from('client_onboarding_requests').select('*').order('created_at', { ascending: false }).limit(25);
  const requests = ((data ?? []) as OnboardingRequest[]);
  const openRequests = requests.filter((request) => !['live', 'paused'].includes(request.status)).length;
  const readyForInvite = requests.filter((request) => request.status === 'admin_invite_ready').length;
  const liveCount = requests.filter((request) => request.status === 'live').length;
  const gapItems: AdminGapItem[] = error ? [{ icon: '🗄️', text: 'Apply onboarding migration', href: '/admin/client-onboarding' }] : [];
  return <AdminSettingsShell active="client-onboarding" organizationName={organization.name} missingCount={gapItems.length} sectionTitle="Client onboarding" gapItems={gapItems}><AdminPageHero title="Client Onboarding" description="Collect a client intake form, create companyname.setuflowcrm.com, preload editable workflow defaults, then send the first admin invitation. Product categories stay client-created." badge="Setup command center" cta={<Link href="/onboarding" className="inline-flex min-h-12 items-center justify-center rounded-2xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white hover:bg-slate-800">Open client form</Link>} stats={[{ label: 'Open requests', value: openRequests, tone: openRequests ? 'warning' : 'success' }, { label: 'Ready for invite', value: readyForInvite, tone: readyForInvite ? 'info' : 'default' }, { label: 'Live clients', value: liveCount, tone: liveCount ? 'success' : 'default' }]} />{searchParams?.notice ? <StateMessage title="Onboarding notice" description={formatStatus(searchParams.notice.replace(/-/g, '_'))} tone="success" /> : null}{error ? <StateMessage title="Client onboarding table is not available yet" description="Apply supabase/migrations/20260503_client_onboarding_requests.sql, then reopen this page to manage submitted intake forms." tone="warning" /> : null}<SectionCard eyebrow="Operating model" title="How client setup works" description="This keeps onboarding controlled: the client submits requirements, Setu Flow performs initial setup, then the client receives the first admin login."><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><AdminStep number="01" title="Client submits form" description="Company identity, first admin, markets, countries, desired pipelines, stages, next steps, pricing notes, and trade event preference." href="/onboarding" /><AdminStep number="02" title="Setu Flow drafts workspace" description="Create the organization with Setu Flow logo fallback and reserve companyname.setuflowcrm.com as the workspace domain." /><AdminStep number="03" title="Preload editable defaults" description="Current pipelines, pipeline stages, next steps, markets, and countries are copied in. Admin can edit or remove them later." /><AdminStep number="04" title="Send first admin login" description="Use Invitations to send the primary admin link after setup is reviewed. Product categories are created by the client." href="/admin/invitations" /></div></SectionCard><SectionCard eyebrow="Default setup package" title="What gets preloaded" description="These are defaults, not locked templates. They can be edited or removed after the first admin login."><div className="grid gap-4 md:grid-cols-2"><ListBlock title="Pipeline stages" items={fallbackStages} /><ListBlock title="Pipelines" items={fallbackPipelines} /><ListBlock title="Next steps" items={fallbackNextSteps} /><ListBlock title="Markets" items={fallbackMarkets} /></div></SectionCard><SectionCard eyebrow="Requests" title="Submitted onboarding forms" description="Review intake, draft the workspace, set status, and move to invitation when ready.">{requests.length === 0 ? <div className="rounded-3xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center"><p className="text-lg font-semibold text-slate-950">No client onboarding requests yet</p><p className="mt-2 text-sm leading-6 text-slate-600">Share the public onboarding form with the next client. Submissions appear here after the migration is applied.</p><Link href="/onboarding" className="mt-5 inline-flex min-h-11 items-center rounded-2xl bg-slate-950 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800">Open client form</Link></div> : <div className="space-y-4">{requests.map((request) => <RequestCard key={request.id} request={request} />)}</div>}</SectionCard></AdminSettingsShell>;
}
