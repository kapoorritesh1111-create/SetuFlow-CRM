import { StateMessage } from '@/components/ui/state-message';
export default function ClientOnboardingLoading() { return <StateMessage title="Loading client onboarding" description="Preparing submitted requests, workspace defaults, and launch actions." />; }
