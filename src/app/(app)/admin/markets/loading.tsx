import { RouteLoadingState } from '@/components/app/route-loading-state';
export default function Loading() { return <RouteLoadingState eyebrow="Admin & Settings" title="Loading markets" description="Preparing organization-scoped admin controls." />; }
