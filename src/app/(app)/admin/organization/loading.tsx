import { RouteLoadingState } from '@/components/app/route-loading-state';

export default function Loading() {
  return <RouteLoadingState eyebrow="Admin" title="Loading organization workspace" description="Preparing governance readiness, admin summaries, and cross-links into the hardened operational surfaces." />;
}
