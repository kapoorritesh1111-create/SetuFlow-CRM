import { BookDemoMarketingPage } from '@/components/marketing/public-pages';

export const metadata = {
  alternates: { canonical: 'https://www.setuflowcrm.com/book-demo' },
};

export default function BookDemoPage() {
  return <BookDemoMarketingPage />;
}
