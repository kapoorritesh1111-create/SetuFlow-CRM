function renderOverview(){const h=$('view-overview');h.innerHTML=`<div class="grid2"><div class="card"><div class="cardh"><div><h2>The full client journey</h2><p>Each step creates something the next step reuses.</p></div></div><div class="cardb stack"><div class="notice success"><b>Correct sequence:</b> Family → Product Variation → Shared Master → Pricing Recipe → Quote. A rate exists once in a Master; recipes reference it; quotes snapshot the result.</div>${[['1','Service Family','Defines what is being sold and which pricing engine applies.'],['2','Product Variations','Defines approved physical sizes/SKUs — no COGS duplicated here.'],['3','Shared Cost & Charge Masters','Creates reusable material/process/extra/pre/post items and rates with Applies To families.'],['4','Pricing Template / Recipe','Chooses Master items and defines geometry, consumption, wastage, margin and output unit.'],['5','Quote Builder','Sales chooses approved configuration; SETU Flow executes the recipe and attaches KLD.']].map(x=>`<div class="card" style="margin:0"><div class="cardb" style="display:grid;grid-template-columns:38px 1fr;gap:10px"><span class="stepno">${x[0]}</span><div><b style="color:var(--navy)">${x[1]}</b><div class="help" style="font-size:9px">${x[2]}</div></div></div></div>`).join('')}<div class="toolbar"><button class="btn teal" data-go="families">Start the journey →</button></div></div></div><aside class="card"><div class="cardh"><div><h3>Source coverage</h3><p>What the client can test now.</p></div></div><div class="cardb stack"><div class="notice"><b>Stand Up Pouches</b><br>Exact 160×230 sample formula reproduces ₹15.836053/pouch at 5,000 with CMYKW + zipper.</div><div class="notice"><b>Center Seal</b><br>96 client constructions loaded. Dimensions calculate pouches/frame; Q1–Q5 values are shown as provisional frame-rate lookups pending unit confirmation.</div><div class="notice"><b>3 Side Seal</b><br>48 Roll Form + 48 Pouch Form constructions loaded with their separate geometry paths.</div><div class="notice warn"><b>Labels / Shrink Sleeves</b><br>Architecture shown, pricing formula not invented because the supplied sources do not define it yet.</div></div></aside></div>`}
function renderFamilies(){const h=$('view-families');h.innerHTML=`<div class="card"><div class="cardh"><div><h2>1 · Service Families</h2><p>Packaging family is the parent. Sizes and recipes live underneath it.</p></div><button class="btn secondary" id="addFamily">+ Add Service Family</button></div><div class="cardb"><div class="familyGrid">${APP_STATE.families.map(f=>`<div class="familyCard ${APP_STATE.family===f.id?'active':''}" data-family="${f.id}"><div class="familyIcon">${icon(f.id)}</div><h4>${esc(f.name)}</h4><p>${esc(f.desc)}</p><span class="tag">${esc(f.mode)}</span><span class="tag ${f.id==='sup'?'green':'amber'}">${esc(f.pricing)}</span></div>`).join('')}</div><div class="card" style="margin:14px 0 0"><div class="cardh"><div><h3>Edit selected family</h3><p>Client can see where families are created and maintained.</p></div></div><div class="cardb grid3" id="familyEditor"></div></div></div></div>`;renderFamilyEditor()}
function renderFamilyEditor(){const f=APP_STATE.families.find(x=>x.id===APP_STATE.family);$('familyEditor').innerHTML=`<div class="field admin"><span>Family name</span><input id="famName" value="${esc(f.name)}"></div><div class="field admin"><span>Product variation mode</span><select id="famMode"><option ${f.mode==='Approved sizes'?'selected':''}>Approved sizes</option><option ${f.mode==='Dynamic dimensions'?'selected':''}>Dynamic dimensions</option><option ${f.mode==='Approved / custom'?'selected':''}>Approved / custom</option><option ${f.mode==='Dynamic / SKU'?'selected':''}>Dynamic / SKU</option><option ${f.mode==='Dynamic'?'selected':''}>Dynamic</option></select></div><div class="field admin"><span>Pricing method</span><select id="famPricing"><option ${f.pricing==='Formula recipe'?'selected':''}>Formula recipe</option><option ${f.pricing.includes('matrix')?'selected':''}>Client matrix + geometry</option><option ${f.pricing==='Roll + Pouch matrix'?'selected':''}>Roll + Pouch matrix</option><option ${f.pricing==='Separate template'?'selected':''}>Separate template</option></select></div>`;$('famName').onchange=e=>{f.name=e.target.value;render()};$('famMode').onchange=e=>{f.mode=e.target.value;render()};$('famPricing').onchange=e=>{f.pricing=e.target.value;render()}}
function renderProducts(){const h=$('view-products');const fam=APP_STATE.family;h.innerHTML=`<div class="card"><div class="cardh"><div><h2>2 · Product Variations</h2><p>Physical sellable/approved variations only. Micron and rates do not live here.</p></div><div class="toolbar"><select id="productFamily">${APP_STATE.families.map(f=>`<option value="${f.id}" ${f.id===fam?'selected':''}>${esc(f.name)}</option>`).join('')}</select><button class="btn secondary" id="addVariation">+ Add Variation</button></div></div><div class="cardb">${fam==='sup'?supProductTable():dynamicProductMessage(fam)}</div></div>`}
function supProductTable(){return `<div class="notice success"><b>Important:</b> PE micron is no longer stored on the product. Product Variations define capacity + physical dimensions. The Pricing Template selects the correct PE COGS Master item for each approved product.</div><div class="tableWrap" style="margin-top:12px"><table><thead><tr><th>Product</th><th>Width mm</th><th>Height mm</th><th>Bottom gusset each mm</th><th>KLD mapping</th></tr></thead><tbody>${APP_STATE.products.sup.map((p,i)=>`<tr><td><input class="cell admin" data-prod="${i}" data-key="1" value="${esc(p[1])}"></td><td><input class="cell admin" type="number" data-prod="${i}" data-key="2" value="${p[2]}"></td><td><input class="cell admin" type="number" data-prod="${i}" data-key="3" value="${p[3]}"></td><td><input class="cell admin" type="number" data-prod="${i}" data-key="4" value="${p[4]}"></td><td><span class="tag green">Sample KLD by size</span></td></tr>`).join('')}</tbody></table></div>`}
function dynamicProductMessage(fam){const f=APP_STATE.families.find(x=>x.id===fam);return `<div class="grid2"><div class="notice"><b>${esc(f.name)}</b><br>The supplied source does not provide a fixed approved-size catalog for this family. The current template is dimension-driven, so Sales enters/chooses dimensions inside the Quote Builder.</div><div class="notice warn"><b>Optional future enhancement</b><br>Admin can still create approved/common variations here later. This should not be invented from the current workbook.</div></div>`}
function renderMasters(){const h=$('view-masters');const tab=APP_STATE.masterTab;h.innerHTML=`<div class="card"><div class="cardh"><div><h2>3 · Shared Cost & Charge Masters</h2><p>Organization-wide reusable library. Pricing Templates reference these IDs; they do not copy the rates.</p></div></div><div class="cardb"><div class="notice success"><b>Shared across families:</b> every Master item has an <b>Applies To</b> list. 12 PET, PE, Adhesive, Lamination, Slitting, etc. can be reused by SUP, Flat Bottom, Center Seal and 3SS wherever applicable.</div><div class="masterTabs" style="margin-top:12px">${[['cogs','COGS Master'],['extras','Extras'],['pre','Pre-production'],['post','Post-production']].map(x=>`<button class="chipbtn ${tab===x[0]?'active':''}" data-master="${x[0]}">${x[1]}</button>`).join('')}</div><div id="masterBody"></div></div></div>`;renderMasterBody()}
function renderMasterBody(){const body=$('masterBody'),tab=APP_STATE.masterTab;if(tab==='cogs'){body.innerHTML=`<div class="toolbar" style="justify-content:space-between;margin-bottom:8px"><div class="help">Yellow fields are Stark Admin maintained. Micron/GSM and rate are editable here.</div><button class="btn secondary" id="addCogs">+ Add COGS Item</button></div><div class="tableWrap"><table><thead><tr><th>Name</th><th>Type</th><th>Micron / GSM</th><th>Density</th><th>Rate unit</th><th>Admin rate</th><th>Applies to families</th></tr></thead><tbody>${APP_STATE.masters.cogs.map((m,i)=>`<tr><td><input class="cell admin" data-cogs="${i}" data-field="name" value="${esc(m.name)}"></td><td><input class="cell" value="${esc(m.kind)}" readonly></td><td><input class="cell admin" type="number" step=".1" data-cogs="${i}" data-field="spec" value="${m.micron??m.gsm??''}"></td><td><input class="cell admin" type="number" step=".001" data-cogs="${i}" data-field="density" value="${m.density??''}"></td><td><input class="cell" value="${esc(m.unit)}" readonly></td><td><input class="cell admin" type="number" step=".01" data-cogs="${i}" data-field="rate" value="${m.rate??''}" placeholder="Confirm"></td><td><input class="cell admin" data-cogs="${i}" data-field="families" value="${esc(m.families.join(', '))}"></td></tr>`).join('')}</tbody></table></div>`}else{const arr=APP_STATE.masters[tab];body.innerHTML=`<div class="toolbar" style="justify-content:space-between;margin-bottom:8px"><div class="help">Names, charging basis, rate and applicable families are created here and then referenced by Pricing Templates / Quotes.</div><button class="btn secondary" id="addCharge">+ Add ${tab==='extras'?'Extra':tab==='pre'?'Pre-production':'Post-production'} Item</button></div><div class="tableWrap"><table><thead><tr><th>Name</th><th>Basis</th><th>Admin rate</th>${tab==='extras'?'<th>Treatment</th>':''}<th>Applies to families</th></tr></thead><tbody>${arr.map((m,i)=>`<tr><td><input class="cell admin" data-charge="${tab}:${i}" data-field="name" value="${esc(m.name)}"></td><td><select class="cell admin" data-charge="${tab}:${i}" data-field="basis"><option value="per_unit" ${m.basis==='per_unit'?'selected':''}>Per unit</option><option value="per_frame" ${m.basis==='per_frame'?'selected':''}>Per frame</option><option value="per_m" ${m.basis==='per_m'?'selected':''}>Per running metre</option><option value="per_kg" ${m.basis==='per_kg'?'selected':''}>Per kg</option><option value="per_job" ${m.basis==='per_job'?'selected':''}>Per job</option></select></td><td><input class="cell admin" type="number" step=".01" data-charge="${tab}:${i}" data-field="rate" value="${m.rate??''}" placeholder="Confirm"></td>${tab==='extras'?`<td><select class="cell admin" data-charge="${tab}:${i}" data-field="treatment"><option value="production" ${m.treatment==='production'?'selected':''}>Before wastage/margin</option><option value="included" ${m.treatment==='included'?'selected':''}>Included after core price</option><option value="separate" ${m.treatment==='separate'?'selected':''}>Separate quote line</option></select></td>`:''}<td><input class="cell admin" data-charge="${tab}:${i}" data-field="families" value="${esc(m.families.join(', '))}"></td></tr>`).join('')}</tbody></table></div>`}}
function renderTemplates(){const h=$('view-templates');const t=APP_STATE.template;h.innerHTML=`<div class="card"><div class="cardh"><div><h2>4 · Pricing Templates / Recipe Builder</h2><p>Recipes reference Shared Master items and define how they are consumed.</p></div></div><div class="cardb"><div class="notice success"><b>Pricing Template ≠ rate master.</b> A rate exists once in Step 3. This step decides which Master items a family uses, the geometry/consumption rule, when extras enter, and how commercial rules produce the selling price.</div><div class="templateTabs" style="margin-top:12px">${[['sup','Stand Up Pouches'],['center','Center Seal'],['three_roll','3SS Roll Form'],['three_pouch','3SS Pouch Form']].map(x=>`<button class="chipbtn ${t===x[0]?'active':''}" data-template="${x[0]}">${x[1]}</button>`).join('')}</div><div id="templateBody"></div></div></div>`;renderTemplateBody()}
function renderTemplateBody(){const body=$('templateBody'),t=APP_STATE.template;if(t==='sup')renderSupRecipe(body);else renderMatrixRecipe(body,t)}
function renderSupRecipe(body){const q=APP_STATE.quote,r=supCalc(q.product,q.qty,q.finish,q.print,q.extraIds);const finish=APP_STATE.templates.sup.finishes[q.finish],p=prod(q.product),peId=finish.peRule[p[0]];body.innerHTML=`<div class="recipeBuilder"><div><div class="grid4"><div class="field admin"><span>Machine width mm</span><input id="tplMW" type="number" value="${APP_STATE.templates.sup.machineW}"></div><div class="field admin"><span>Machine length mm</span><input id="tplML" type="number" value="${APP_STATE.templates.sup.machineL}"></div><div class="field admin"><span>Trim allowance mm</span><input id="tplTrim" type="number" value="${APP_STATE.templates.sup.trim}"></div><div class="field admin"><span>Outer print web mm</span><input id="tplOuter" type="number" value="${APP_STATE.templates.sup.outerWeb}"></div></div><div class="grid3" style="margin:12px 0"><div class="field"><span>Test product</span><select id="tplProduct">${APP_STATE.products.sup.map(x=>`<option value="${x[0]}" ${q.product===x[0]?'selected':''}>${esc(x[1])} · ${x[2]}×${x[3]}</option>`).join('')}</select></div><div class="field"><span>Construction recipe</span><select id="tplFinish">${Object.entries(APP_STATE.templates.sup.finishes).map(([k,v])=>`<option value="${k}" ${q.finish===k?'selected':''}>${esc(v.label)}</option>`).join('')}</select></div><div class="field"><span>Test quantity</span><select id="tplQty">${[2000,3000,5000,10000,20000,30000,50000].map(x=>`<option value="${x}" ${q.qty===x?'selected':''}>${x.toLocaleString('en-IN')}</option>`).join('')}</select></div></div><div class="recipeRows"><div class="recipeRow header"><span>Source</span><span>Master item</span><span>Recipe role</span><span>Consumption rule</span><span>Current rate</span></div>${finish.layers.map(id=>recipeLayerRow(id,'Material layer',id==='metpet12'||id==='clearpet12'?'GSM × inner stock web × frame length':'GSM × outer print web × frame length')).join('')}${recipeLayerRow(peId,'Inner PE layer','GSM × PE stock web × frame length')}${recipeLayerRow('adh','Bonding','Bond GSM × PE web × frame length')}<div class="recipeRow"><span class="source">COGS Master</span><b>${esc(getCogs(q.print).name)}</b><span>Printing</span><span>Per frame</span><span class="rate">${money(getCogs(q.print).rate)}</span></div>${['lam','slit','pouch'].map(id=>recipeLayerRow(id,'Process','Rate × running metres/frame')).join('')}<div class="recipeRow"><span class="source">Extras Master</span><b>Zipper</b><span>Production extra when selected</span><span>₹/running metre before wastage/margin</span><span class="rate">${money(getExtra('zipper').rate)}</span></div></div><div class="notice" style="margin-top:10px"><b>PE selection rule lives in the Pricing Template, not Product Variation:</b> this product currently maps to <b>${esc(getCogs(peId).name)}</b>. Admin can change the PE Master item used by any approved product below.</div><div class="tableWrap" style="margin-top:10px"><table><thead><tr><th>Approved SUP product</th><th>PE COGS Master item used by recipe</th></tr></thead><tbody>${APP_STATE.products.sup.map(x=>`<tr><td>${esc(x[1])} · ${x[2]}×${x[3]}</td><td><select class="cell admin" data-pe-map="${x[0]}">${['pe60','pe75','pe95','pe120'].map(id=>`<option value="${id}" ${finish.peRule[x[0]]===id?'selected':''}>${esc(getCogs(id).name)} · ${money(getCogs(id).rate)}/kg</option>`).join('')}</select></td></tr>`).join('')}</tbody></table></div><div class="card" style="margin:12px 0 0"><div class="cardh"><div><h3>Run-length commercial rules</h3><p>Editable recipe control from the client sample.</p></div></div><div class="cardb tableWrap"><table><thead><tr><th>Run length up to</th><th>Wastage</th><th>Margin / frame</th></tr></thead><tbody>${APP_STATE.templates.sup.bands.map((b,i)=>`<tr><td><input class="cell admin" data-band="${i}" data-bf="max" type="number" value="${b.max}"> m</td><td><input class="cell admin" data-band="${i}" data-bf="waste" type="number" value="${b.waste}">%</td><td>₹ <input class="cell admin" data-band="${i}" data-bf="margin" type="number" value="${b.margin}"></td></tr>`).join('')}</tbody></table></div></div></div><aside>${r.ok?calcBox(r):`<div class="notice error">${esc(r.error)}</div>`}<div class="notice" style="margin-top:10px"><b>Where to edit values:</b><br>Micron, density and material/process rates → <b>Step 3 Shared COGS Master</b>.<br>Which items are used + PE mapping + wastage/margin → <b>this Pricing Template recipe</b>.<br>What Sales selects → <b>Quote Builder</b>.</div></aside></div>`}