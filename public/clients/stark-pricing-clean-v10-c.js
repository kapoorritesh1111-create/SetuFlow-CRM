function recipeLayerRow(id,role,rule){const m=getCogs(id);return `<div class="recipeRow"><span class="source">COGS Master</span><b>${esc(m.name)}</b><span>${esc(role)}</span><span>${esc(rule)}</span><span class="rate">${money(m.rate)} ${esc(m.unit.replace('₹/','/'))}</span></div>`}
function calcBox(r){return `<div class="calcBox"><b style="color:#18552f">Live recipe calculation</b><div class="calcGrid" style="margin-top:8px"><div class="calcCell"><span>Geometry</span><b>${r.ac} × ${r.al} = ${r.ppf}/frame</b></div><div class="calcCell"><span>Run length</span><b>${r.run.toFixed(0)} m</b></div><div class="calcCell"><span>Material COGS</span><b>${money(r.materialCost)}/frame</b></div><div class="calcCell"><span>Process COGS</span><b>${money(r.process)}/frame</b></div><div class="calcCell"><span>Production extras</span><b>${money(r.productionExtra)}/frame</b></div><div class="calcCell"><span>Waste + margin</span><b>${r.band.waste}% + ${money(r.band.margin)}</b></div><div class="calcCell"><span>Sell / frame</span><b>${money(r.sellFrame)}</b></div><div class="calcCell"><span>Final / pouch</span><b>${money(r.unit)}</b></div></div><div class="formula">(${money(r.materialCost)} materials + ${money(r.process)} processes + ${money(r.productionExtra)} production extras) + ${r.band.waste}% wastage (${money(r.waste)}) + ${money(r.band.margin)} margin = ${money(r.sellFrame)}/frame ÷ ${r.ppf} pouches/frame = <b>${money(r.unit)}/pouch</b>.<br><br>${r.p[0]==='p8'&&r.qty===5000?'✅ Client calibration target reproduced: approximately ₹15.836053/pouch.':''}</div></div>`}
function renderMatrixRecipe(body,kind){const isCenter=kind==='center',isPouch=kind==='three_pouch';const title=isCenter?'Center Seal':isPouch?'3 Side Seal · Pouch Form':'3 Side Seal · Roll Form',q=isCenter?APP_STATE.quote.center:APP_STATE.quote.three,calc=matrixCalc(kind,q.w,q.h,q.spec,q.tier);body.innerHTML=`<div class="recipeBuilder"><div><div class="notice"><b>${title} current source:</b> client workbook matrix + geometry. Shared COGS Master materials can support specification/weight/yield, but the supplied Q1–Q5 values currently drive the commercial output until Stark confirms their unit/meaning.</div><div class="grid4" style="margin-top:10px"><div class="field"><span>Width mm</span><input id="mxW" type="number" value="${q.w}"></div><div class="field"><span>Height mm</span><input id="mxH" type="number" value="${q.h}"></div><div class="field"><span>Construction</span><select id="mxSpec">${APP_STATE.rateData[kind].map((r,i)=>`<option value="${i}" ${Number(q.spec)===i?'selected':''}>${esc(r[1])} · ${esc(r[0])}</option>`).join('')}</select></div><div class="field"><span>Frame tier</span><select id="mxTier">${[250,500,1000,2000,3000].map(x=>`<option value="${x}" ${q.tier===x?'selected':''}>${x.toLocaleString('en-IN')} frames</option>`).join('')}</select></div></div><div class="recipeRows" style="margin-top:10px"><div class="recipeRow header"><span>Step</span><span>Recipe input</span><span>Source</span><span>Rule</span><span>Output</span></div><div class="recipeRow"><span class="source">1</span><b>Requested dimensions</b><span>Quote Builder</span><span>${isPouch?'Open laminate = 2 × height + 12mm':'Machine layout from width × height'}</span><span class="rate">${q.w}×${q.h}</span></div><div class="recipeRow"><span class="source">2</span><b>Construction spec</b><span>Client matrix + Shared COGS reference</span><span>Material stack defines GSM / yield reference</span><span class="rate">${calc.ok?calc.row[1]:'—'}</span></div><div class="recipeRow"><span class="source">3</span><b>Machine geometry</b><span>Pricing Template</span><span>740×1120 layout → pouches/frame</span><span class="rate">${calc.ok?calc.ppf:'—'}</span></div><div class="recipeRow"><span class="source">4</span><b>Q1–Q5 rate</b><span>Client workbook</span><span>Lookup selected frame tier</span><span class="rate">${calc.ok?money(calc.frameRate):'—'}</span></div><div class="recipeRow"><span class="source">5</span><b>Provisional unit sell</b><span>Pricing Template</span><span>Current interpretation: frame rate ÷ pouches/frame</span><span class="rate">${calc.ok?money(calc.base):'—'}</span></div></div></div><aside>${calc.ok?`<div class="calcBox"><b style="color:#18552f">Live ${title} test</b><div class="calcGrid" style="margin-top:8px"><div class="calcCell"><span>Across × along</span><b>${calc.ac} × ${calc.al}</b></div><div class="calcCell"><span>Pouches/frame</span><b>${calc.ppf}</b></div><div class="calcCell"><span>MOQ at selected tier</span><b>${calc.qty.toLocaleString('en-IN')} pcs</b></div><div class="calcCell"><span>Matrix rate</span><b>${money(calc.frameRate)}</b></div><div class="calcCell"><span>Provisional ₹/pouch</span><b>${money(calc.base)}</b></div><div class="calcCell"><span>Yield reference</span><b>${calc.yieldKg?calc.yieldKg.toFixed(0)+' pcs/kg':'Need GSM'}</b></div></div>${calc.unknown.length?`<div class="notice warn" style="margin-top:8px">Need source confirmation: ${esc(calc.unknown.join(', '))}.</div>`:''}</div>`:`<div class="notice error">${esc(calc.error)}</div>`}<div class="notice warn" style="margin-top:10px"><b>Client confirmation required:</b> Q1–Q5 are currently interpreted as commercial ₹/frame values because the workbook tiers are frame counts. The Questions tab explicitly asks Stark to confirm this before production implementation.</div></aside></div>`}
function renderQuote(){const h=$('view-quote');const fam=APP_STATE.quote.family;h.innerHTML=`<div class="quoteGrid"><div><div class="card"><div class="cardh"><div><h2>5 · Quote Builder</h2><p>Sales consumes approved families, products, recipes and charge masters — never internal COGS.</p></div></div><div class="cardb stack"><div class="grid3"><div class="field"><span>Service Family</span><select id="qFamily"><option value="sup" ${fam==='sup'?'selected':''}>Stand Up Pouches</option><option value="center" ${fam==='center'?'selected':''}>Center Seal Pouches</option><option value="three" ${fam==='three'?'selected':''}>3 Side Seal Pouches</option></select></div><div id="quoteConfig1"></div><div id="quoteConfig2"></div></div><div id="quoteExtraArea"></div><div class="grid2"><div><h3 style="color:var(--navy);font-size:12px">Pre-production charges</h3><div id="preArea"></div></div><div><h3 style="color:var(--navy);font-size:12px">Post-production charges</h3><div id="postArea"></div></div></div></div></div><div class="card"><div class="cardh"><div><h3>Sample KLD / artwork reference</h3><p>Changes with the selected family and product.</p></div></div><div class="cardb"><div class="kld"><svg id="kldSvg" viewBox="0 0 620 380"></svg></div></div></div></div><aside><div class="pricebox"><small>Calculated selling price</small><span class="big" id="qUnit">—</span><small id="qUnitLabel">per unit</small><div style="height:12px"></div><small>Product total</small><span class="total" id="qProductTotal">—</span><div id="qBreakdown" style="margin-top:12px"></div><div class="line"><span>Pre/Post/Separate charges</span><b id="qSeparate">—</b></div><div class="line"><span>GST</span><b id="qTax">—</b></div><div class="line"><span>Grand total</span><b id="qGrand">—</b></div></div><div id="qWarnings" style="margin-top:10px"></div></aside></div>`;setupQuoteFields();renderQuoteResult()}
function setupQuoteFields(){const fam=APP_STATE.quote.family;if(fam==='sup'){$('quoteConfig1').innerHTML=`<div class="field"><span>Approved product / size</span><select id="qProduct">${APP_STATE.products.sup.map(p=>`<option value="${p[0]}" ${APP_STATE.quote.product===p[0]?'selected':''}>${esc(p[1])} · ${p[2]}×${p[3]} · BG ${p[4]}+${p[4]}</option>`).join('')}</select></div>`;$('quoteConfig2').innerHTML=`<div class="field"><span>Construction</span><select id="qFinish">${Object.entries(APP_STATE.templates.sup.finishes).map(([k,v])=>`<option value="${k}" ${APP_STATE.quote.finish===k?'selected':''}>${esc(v.label)}</option>`).join('')}</select></div>`;$('quoteExtraArea').innerHTML=`<div class="grid3"><div class="field"><span>MOQ / Quantity</span><select id="qQty">${[2000,3000,5000,10000,20000,30000,50000].map(x=>`<option value="${x}" ${APP_STATE.quote.qty===x?'selected':''}>${x.toLocaleString('en-IN')}</option>`).join('')}</select></div><div class="field"><span>Printing</span><select id="qPrint"><option value="printCMYKW" ${APP_STATE.quote.print==='printCMYKW'?'selected':''}>CMYKW</option><option value="printCMYK" ${APP_STATE.quote.print==='printCMYK'?'selected':''}>CMYK</option></select></div><div class="field"><span>GST %</span><input id="qGst" type="number" value="${APP_STATE.quote.gst}"></div></div><h3 style="color:var(--navy);font-size:12px">Extras</h3><div class="addons">${APP_STATE.masters.extras.filter(e=>e.families.includes('sup')).map(e=>addonCard(e)).join('')}</div>`}else if(fam==='center'){$('quoteConfig1').innerHTML=`<div class="field"><span>Width mm</span><input id="qCW" type="number" value="${APP_STATE.quote.center.w}"></div>`;$('quoteConfig2').innerHTML=`<div class="field"><span>Height mm</span><input id="qCH" type="number" value="${APP_STATE.quote.center.h}"></div>`;$('quoteExtraArea').innerHTML=`<div class="grid3"><div class="field"><span>Construction</span><select id="qCSpec">${APP_STATE.rateData.center.map((r,i)=>`<option value="${i}" ${APP_STATE.quote.center.spec===i?'selected':''}>${esc(r[1])} · ${esc(r[0])}</option>`).join('')}</select></div><div class="field"><span>Frame tier</span><select id="qCTier">${[250,500,1000,2000,3000].map(x=>`<option value="${x}" ${APP_STATE.quote.center.tier===x?'selected':''}>${x.toLocaleString('en-IN')} frames</option>`).join('')}</select></div><div class="field"><span>GST %</span><input id="qGst" type="number" value="${APP_STATE.quote.gst}"></div></div>`}else{$('quoteConfig1').innerHTML=`<div class="field"><span>Supply Form</span><select id="q3Form"><option value="roll" ${APP_STATE.quote.three.form==='roll'?'selected':''}>Roll Form</option><option value="pouch" ${APP_STATE.quote.three.form==='pouch'?'selected':''}>Pouch Form</option></select></div>`;$('quoteConfig2').innerHTML=`<div class="field"><span>Width × Height mm</span><div class="grid2"><input id="q3W" type="number" value="${APP_STATE.quote.three.w}"><input id="q3H" type="number" value="${APP_STATE.quote.three.h}"></div></div>`;const kind=APP_STATE.quote.three.form==='pouch'?'three_pouch':'three_roll';$('quoteExtraArea').innerHTML=`<div class="grid3"><div class="field"><span>Construction</span><select id="q3Spec">${APP_STATE.rateData[kind].map((r,i)=>`<option value="${i}" ${APP_STATE.quote.three.spec===i?'selected':''}>${esc(r[1])} · ${esc(r[0])}</option>`).join('')}</select></div><div class="field"><span>Frame tier</span><select id="q3Tier">${[250,500,1000,2000,3000].map(x=>`<option value="${x}" ${APP_STATE.quote.three.tier===x?'selected':''}>${x.toLocaleString('en-IN')} frames</option>`).join('')}</select></div><div class="field"><span>GST %</span><input id="qGst" type="number" value="${APP_STATE.quote.gst}"></div></div>`}renderChargeSelectors()}
function addonCard(e){return `<div class="addon"><label><input type="checkbox" data-q-extra="${e.id}" ${APP_STATE.quote.extraIds.has(e.id)?'checked':''}><span><b>${esc(e.name)}</b><small>${e.rate==null?'Rate to confirm':money(e.rate)} · ${esc(e.basis.replace('per_','per '))} · ${esc(e.treatment)}</small></span></label></div>`}
function renderChargeSelectors(){for(const group of ['pre','post']){$(group+'Area').innerHTML=APP_STATE.masters[group].map(e=>`<div class="addon"><label><input type="checkbox" data-q-${group}="${e.id}" ${APP_STATE.quote[group+'Ids'].has(e.id)?'checked':''}><span><b>${esc(e.name)}</b><small>${money(e.rate)} · ${esc(e.basis.replace('per_','per '))}</small></span></label></div>`).join('')}}
function renderQuoteResult(){let ctx,baseUnit,qty,core,fam=APP_STATE.quote.family;if(fam==='sup'){core=supCalc(APP_STATE.quote.product,APP_STATE.quote.qty,APP_STATE.quote.finish,APP_STATE.quote.print,APP_STATE.quote.extraIds);if(!core.ok)return quoteError(core.error);baseUnit=core.unit;qty=core.qty;ctx={qty,frames:core.frames,run:core.run,weightKg:core.weightPerUnit*qty/1000};$('qUnitLabel').textContent='per pouch'}else if(fam==='center'){core=matrixCalc('center',APP_STATE.quote.center.w,APP_STATE.quote.center.h,APP_STATE.quote.center.spec,APP_STATE.quote.center.tier);if(!core.ok)return quoteError(core.error);baseUnit=core.base;qty=core.qty;ctx={qty,frames:core.frames,run:null,weightKg:core.weight*qty/1000};$('qUnitLabel').textContent='provisional per pouch'}else{const kind=APP_STATE.quote.three.form==='pouch'?'three_pouch':'three_roll';core=matrixCalc(kind,APP_STATE.quote.three.w,APP_STATE.quote.three.h,APP_STATE.quote.three.spec,APP_STATE.quote.three.tier);if(!core.ok)return quoteError(core.error);baseUnit=core.base;qty=core.qty;ctx={qty,frames:core.frames,run:null,weightKg:core.weight*qty/1000};$('qUnitLabel').textContent=APP_STATE.quote.three.form==='roll'?'provisional per piece-equivalent':'provisional per pouch'}const post=addPostCharges(ctx);let unit=baseUnit+post.included,productTotal=unit*qty,tax=(productTotal+post.separate)*(APP_STATE.quote.gst/100),grand=productTotal+post.separate+tax;$('qUnit').textContent=money(unit);$('qProductTotal').textContent=money(productTotal);$('qSeparate').textContent=money(post.separate);$('qTax').textContent=money(tax);$('qGrand').textContent=money(grand);$('qBreakdown').innerHTML=fam==='sup'?`<div class="line"><span>Core recipe price</span><b>${money(core.unit)}/pouch</b></div><div class="line"><span>Run length</span><b>${core.run.toFixed(0)} m</b></div><div class="line"><span>Wastage + margin</span><b>${core.band.waste}% + ${money(core.band.margin)}/frame</b></div>`:`<div class="line"><span>Pouches / frame</span><b>${core.ppf}</b></div><div class="line"><span>Client matrix value</span><b>${money(core.frameRate)}</b></div><div class="line"><span>Current interpretation</span><b>Matrix value ÷ pouches/frame</b></div>`;$('qWarnings').innerHTML=post.missing.length?`<div class="notice warn">Selected extras need a confirmed rate: ${esc(post.missing.join(', '))}</div>`:'';renderKld(fam,core)}
function quoteError(msg){$('qUnit').textContent='Blocked';$('qProductTotal').textContent='—';$('qSeparate').textContent='—';$('qTax').textContent='—';$('qGrand').textContent='—';$('qWarnings').innerHTML=`<div class="notice error">${esc(msg)}</div>`}
function renderKld(fam,core){const s=$('kldSvg');if(!s)return;if(fam==='sup'){const p=core.p;s.innerHTML=`<rect width="620" height="380" rx="20" fill="#fff"/><path d="M220 55h180l28 245c0 16-14 28-30 28H222c-16 0-30-12-30-28l28-245Z" fill="#eef3f8" stroke="#8fa5b7" stroke-width="2"/><path d="M232 80h156" stroke="#8fa5b7" stroke-width="4"/><path d="M255 270c25-18 85-18 110 0" fill="none" stroke="#adbccb" stroke-width="2"/><text x="225" y="30" font-size="20" font-weight="800" fill="#0b2948">Sample SUP KLD</text><text x="255" y="358" font-size="17" fill="#36516c">${p[1]} · ${p[2]}×${p[3]} · BG ${p[4]}+${p[4]}</text>`}else if(fam==='center'){s.innerHTML=`<rect width="620" height="380" rx="20" fill="#fff"/><rect x="190" y="60" width="240" height="250" rx="12" fill="#eef3f8" stroke="#8fa5b7" stroke-width="2"/><line x1="310" y1="60" x2="310" y2="310" stroke="#718ca4" stroke-width="2" stroke-dasharray="7 6"/><text x="205" y="32" font-size="20" font-weight="800" fill="#0b2948">Center Seal Layout Reference</text><text x="245" y="345" font-size="17" fill="#36516c">${core.w}×${core.h} mm · ${core.ppf} pcs/frame</text>`}else{s.innerHTML=`<rect width="620" height="380" rx="20" fill="#fff"/><rect x="150" y="95" width="320" height="190" rx="10" fill="#eef3f8" stroke="#8fa5b7" stroke-width="2"/><line x1="150" y1="95" x2="470" y2="95" stroke="#718ca4" stroke-width="2" stroke-dasharray="7 6"/><line x1="150" y1="285" x2="470" y2="285" stroke="#718ca4" stroke-width="2" stroke-dasharray="7 6"/><text x="180" y="38" font-size="20" font-weight="800" fill="#0b2948">3 Side Seal Layout Reference</text><text x="245" y="330" font-size="17" fill="#36516c">${core.w}×${core.h} mm · ${core.ppf} pcs/frame</text>`}}