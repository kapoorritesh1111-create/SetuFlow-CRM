-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.ai_suggestions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  suggestion_type text NOT NULL,
  content text NOT NULL,
  status text NOT NULL DEFAULT 'generated'::text CHECK (status = ANY (ARRAY['generated'::text, 'reviewed'::text, 'approved'::text, 'dismissed'::text, 'applied'::text])),
  suggested_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  decided_by uuid,
  decided_at timestamp with time zone,
  organization_id uuid NOT NULL,
  target_entity_type text,
  target_entity_id uuid,
  draft_subject text,
  draft_body text,
  rationale text,
  prompt_context jsonb NOT NULL DEFAULT '{}'::jsonb,
  reviewed_by uuid,
  reviewed_at timestamp with time zone,
  decision_outcome text,
  operator_notes text,
  applied_communication_id uuid,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_suggestions_pkey PRIMARY KEY (id),
  CONSTRAINT ai_suggestions_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT ai_suggestions_suggested_by_fkey FOREIGN KEY (suggested_by) REFERENCES public.profiles(id),
  CONSTRAINT ai_suggestions_decided_by_fkey FOREIGN KEY (decided_by) REFERENCES public.profiles(id),
  CONSTRAINT ai_suggestions_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT ai_suggestions_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES public.profiles(id),
  CONSTRAINT ai_suggestions_applied_communication_id_fkey FOREIGN KEY (applied_communication_id) REFERENCES public.communications(id)
);
CREATE TABLE public.audit_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  actor_user_id uuid,
  entity_type text NOT NULL,
  entity_id uuid,
  action text NOT NULL,
  payload jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id),
  CONSTRAINT audit_logs_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT audit_logs_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.communications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid,
  quote_id uuid,
  rfq_id uuid,
  related_entity text NOT NULL DEFAULT 'lead'::text CHECK (related_entity = ANY (ARRAY['lead'::text, 'quote'::text, 'rfq'::text, 'trade_event_entry'::text, 'other'::text])),
  related_id uuid,
  communication_type text NOT NULL DEFAULT 'follow_up'::text CHECK (communication_type = ANY (ARRAY['introduction'::text, 'follow_up'::text, 'quote_message'::text, 'compliance_request'::text, 'system_note'::text, 'other'::text])),
  direction text NOT NULL DEFAULT 'outbound'::text CHECK (direction = ANY (ARRAY['inbound'::text, 'outbound'::text, 'internal'::text])),
  channel text NOT NULL DEFAULT 'email'::text CHECK (channel = ANY (ARRAY['email'::text, 'phone'::text, 'whatsapp'::text, 'linkedin'::text, 'trade_show'::text, 'meeting'::text, 'system'::text, 'other'::text])),
  subject text,
  body text,
  summary text,
  draft_source text NOT NULL DEFAULT 'manual'::text CHECK (draft_source = ANY (ARRAY['manual'::text, 'ai'::text, 'imported'::text, 'system'::text])),
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'approved'::text, 'sent'::text, 'received'::text, 'failed'::text, 'cancelled'::text])),
  sent_at timestamp with time zone,
  scheduled_at timestamp with time zone,
  approved_at timestamp with time zone,
  approved_by uuid,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  provider_message_id text,
  provider_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT communications_pkey PRIMARY KEY (id),
  CONSTRAINT communications_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT communications_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT communications_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT communications_rfq_id_fkey FOREIGN KEY (rfq_id) REFERENCES public.rfqs(id),
  CONSTRAINT communications_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.profiles(id),
  CONSTRAINT communications_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.compliance_checklist_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  country_id uuid NOT NULL,
  code text NOT NULL,
  description text NOT NULL,
  is_mandatory boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT compliance_checklist_items_pkey PRIMARY KEY (id),
  CONSTRAINT compliance_checklist_items_country_id_fkey FOREIGN KEY (country_id) REFERENCES public.countries(id)
);
CREATE TABLE public.contract_line_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  contract_id uuid NOT NULL,
  product_id uuid,
  quantity numeric NOT NULL CHECK (quantity > 0::numeric),
  unit_price numeric CHECK (unit_price >= 0::numeric),
  currency text,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  product_variant_id uuid,
  catalog_price_id uuid,
  catalog_price_amount numeric,
  catalog_price_currency text,
  is_price_overridden boolean NOT NULL DEFAULT false,
  override_reason text,
  overridden_by uuid,
  overridden_at timestamp with time zone,
  source_quote_line_item_id uuid,
  continuity_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT contract_line_items_pkey PRIMARY KEY (id),
  CONSTRAINT contract_line_items_contract_id_fkey FOREIGN KEY (contract_id) REFERENCES public.contracts(id),
  CONSTRAINT contract_line_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT contract_line_items_product_variant_id_fkey FOREIGN KEY (product_variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT contract_line_items_catalog_price_id_fkey FOREIGN KEY (catalog_price_id) REFERENCES public.product_prices(id),
  CONSTRAINT contract_line_items_overridden_by_fkey FOREIGN KEY (overridden_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.contracts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  quote_id uuid NOT NULL,
  lead_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'signed'::text, 'active'::text, 'completed'::text, 'cancelled'::text])),
  signed_at timestamp with time zone,
  starts_on date,
  ends_on date,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  commercial_lock_state text,
  quote_currency text,
  pricing_basis text,
  approval_required boolean NOT NULL DEFAULT false,
  approval_state text NOT NULL DEFAULT 'not_required'::text,
  approved_at timestamp with time zone,
  sent_at timestamp with time zone,
  accepted_at timestamp with time zone,
  locked_at timestamp with time zone,
  commercial_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  execution_state text NOT NULL DEFAULT 'draft'::text,
  execution_blockers jsonb NOT NULL DEFAULT '[]'::jsonb,
  execution_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  ready_at timestamp with time zone,
  released_at timestamp with time zone,
  dispatched_at timestamp with time zone,
  completed_at timestamp with time zone,
  CONSTRAINT contracts_pkey PRIMARY KEY (id),
  CONSTRAINT contracts_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT contracts_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT contracts_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id)
);
CREATE TABLE public.countries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  market_id uuid NOT NULL,
  name text NOT NULL,
  iso2_code text,
  iso3_code text,
  phone_code text,
  sort_order integer DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  search_aliases text,
  CONSTRAINT countries_pkey PRIMARY KEY (id),
  CONSTRAINT countries_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT countries_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id)
);
CREATE TABLE public.document_requirement_rules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  market_id uuid,
  product_id uuid,
  lead_type text,
  progression_scope text NOT NULL DEFAULT 'general'::text,
  requirement_code text NOT NULL,
  title text,
  doc_type text,
  applies_to_entity text NOT NULL DEFAULT 'lead'::text,
  is_mandatory boolean NOT NULL DEFAULT true,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT document_requirement_rules_pkey PRIMARY KEY (id),
  CONSTRAINT document_requirement_rules_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT document_requirement_rules_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id),
  CONSTRAINT document_requirement_rules_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.document_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  document_id uuid NOT NULL,
  version integer NOT NULL,
  file_url text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  CONSTRAINT document_versions_pkey PRIMARY KEY (id),
  CONSTRAINT document_versions_document_id_fkey FOREIGN KEY (document_id) REFERENCES public.documents(id),
  CONSTRAINT document_versions_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.documents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  related_entity text NOT NULL,
  related_id uuid NOT NULL,
  file_name text NOT NULL,
  file_url text NOT NULL,
  doc_type text NOT NULL,
  uploaded_by uuid,
  uploaded_at timestamp with time zone NOT NULL DEFAULT now(),
  version integer NOT NULL DEFAULT 1,
  status text NOT NULL DEFAULT 'uploaded'::text CHECK (status = ANY (ARRAY['uploaded'::text, 'submitted'::text, 'approved'::text, 'rejected'::text, 'archived'::text])),
  owner_user_id uuid,
  reviewer_user_id uuid,
  reviewed_at timestamp with time zone,
  review_notes text,
  expires_at date,
  version_label text,
  requirement_code text,
  CONSTRAINT documents_pkey PRIMARY KEY (id),
  CONSTRAINT documents_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT documents_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES public.profiles(id),
  CONSTRAINT documents_owner_user_id_fkey FOREIGN KEY (owner_user_id) REFERENCES public.profiles(id),
  CONSTRAINT documents_reviewer_user_id_fkey FOREIGN KEY (reviewer_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.exchange_rates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  base_currency text NOT NULL,
  quote_currency text NOT NULL,
  rate numeric NOT NULL CHECK (rate > 0::numeric),
  provider text NOT NULL,
  effective_at timestamp with time zone NOT NULL,
  fetched_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT exchange_rates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.freight_calc_assumptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  freight_profile_id uuid NOT NULL,
  chips_mode text,
  chips_ship_qty numeric,
  powders_mode text,
  powders_ship_qty numeric,
  pallets_per_40ft numeric,
  pallets_per_20ft numeric,
  cases_per_pallet numeric,
  bags_per_case numeric,
  kg_per_pallet numeric,
  twenty_ft_factor numeric,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT freight_calc_assumptions_pkey PRIMARY KEY (id),
  CONSTRAINT freight_calc_assumptions_freight_profile_id_fkey FOREIGN KEY (freight_profile_id) REFERENCES public.freight_profiles(id)
);
CREATE TABLE public.freight_profile_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  freight_profile_id uuid NOT NULL,
  line_no integer NOT NULL,
  particular text NOT NULL,
  input_currency text NOT NULL CHECK (input_currency = ANY (ARRAY['USD'::text, 'INR'::text, 'EUR'::text, 'GBP'::text, 'AED'::text])),
  amount numeric NOT NULL CHECK (amount >= 0::numeric),
  applies_to_container_type text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT freight_profile_items_pkey PRIMARY KEY (id),
  CONSTRAINT freight_profile_items_freight_profile_id_fkey FOREIGN KEY (freight_profile_id) REFERENCES public.freight_profiles(id)
);
CREATE TABLE public.freight_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  market_id uuid,
  country_id uuid,
  destination_port text NOT NULL,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['draft'::text, 'active'::text, 'archived'::text])),
  notes text,
  created_by uuid,
  updated_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT freight_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT freight_profiles_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT freight_profiles_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id),
  CONSTRAINT freight_profiles_country_id_fkey FOREIGN KEY (country_id) REFERENCES public.countries(id),
  CONSTRAINT freight_profiles_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT freight_profiles_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.hs_codes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  code text NOT NULL UNIQUE,
  description text NOT NULL,
  unit_measure text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT hs_codes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.hs_duties (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  hs_code_id uuid NOT NULL,
  country_id uuid NOT NULL,
  duty_rate numeric NOT NULL CHECK (duty_rate >= 0::numeric),
  effective_from date NOT NULL,
  effective_to date,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT hs_duties_pkey PRIMARY KEY (id),
  CONSTRAINT hs_duties_hs_code_id_fkey FOREIGN KEY (hs_code_id) REFERENCES public.hs_codes(id),
  CONSTRAINT hs_duties_country_id_fkey FOREIGN KEY (country_id) REFERENCES public.countries(id)
);
CREATE TABLE public.import_issues (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  import_run_id uuid NOT NULL,
  entity_type text NOT NULL,
  entity_subtype text,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  field_name text,
  source_value text,
  normalized_value text,
  severity text NOT NULL CHECK (severity = ANY (ARRAY['warning'::text, 'error'::text])),
  issue_code text NOT NULL,
  issue_message text NOT NULL,
  suggested_fix text,
  blocking_flag boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT import_issues_pkey PRIMARY KEY (id),
  CONSTRAINT import_issues_import_run_id_fkey FOREIGN KEY (import_run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.import_normalization_rules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  master_table text NOT NULL CHECK (master_table = ANY (ARRAY['markets'::text, 'countries'::text, 'pipeline_stages'::text, 'next_steps'::text, 'product_categories'::text])),
  source_value text NOT NULL,
  normalized_value text,
  action text NOT NULL CHECK (action = ANY (ARRAY['accepted_as_is'::text, 'newly_created'::text, 'manually_correct_before_import'::text])),
  notes text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT import_normalization_rules_pkey PRIMARY KEY (id),
  CONSTRAINT import_normalization_rules_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.import_runs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  import_type text NOT NULL CHECK (import_type = ANY (ARRAY['products'::text, 'leads'::text, 'quotes'::text, 'full'::text])),
  source_file_name text,
  source_file_checksum text,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'running'::text, 'validated'::text, 'completed'::text, 'failed'::text, 'cancelled'::text])),
  started_by uuid,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  rows_read integer NOT NULL DEFAULT 0,
  rows_valid integer NOT NULL DEFAULT 0,
  rows_warning integer NOT NULL DEFAULT 0,
  rows_blocked integer NOT NULL DEFAULT 0,
  rows_inserted integer NOT NULL DEFAULT 0,
  rows_updated integer NOT NULL DEFAULT 0,
  summary_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT import_runs_pkey PRIMARY KEY (id),
  CONSTRAINT import_runs_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT import_runs_started_by_fkey FOREIGN KEY (started_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.integration_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  integration_id uuid NOT NULL,
  direction text NOT NULL CHECK (direction = ANY (ARRAY['inbound'::text, 'outbound'::text])),
  event_type text NOT NULL,
  payload jsonb NOT NULL,
  status text NOT NULL DEFAULT 'received'::text CHECK (status = ANY (ARRAY['received'::text, 'processed'::text, 'error'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  processed_at timestamp with time zone,
  CONSTRAINT integration_events_pkey PRIMARY KEY (id),
  CONSTRAINT integration_events_integration_id_fkey FOREIGN KEY (integration_id) REFERENCES public.integrations(id)
);
CREATE TABLE public.integrations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  provider text NOT NULL,
  configuration jsonb NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT integrations_pkey PRIMARY KEY (id),
  CONSTRAINT integrations_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.lead_activities (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid NOT NULL,
  actor_user_id uuid,
  kind text NOT NULL,
  message text NOT NULL,
  occurred_at timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  legacy_activity_id text,
  CONSTRAINT lead_activities_pkey PRIMARY KEY (id),
  CONSTRAINT lead_activities_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT lead_activities_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_activities_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_assignment_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  from_user_id uuid,
  to_user_id uuid,
  changed_by uuid,
  assigned_at timestamp with time zone NOT NULL DEFAULT now(),
  note text,
  CONSTRAINT lead_assignment_history_pkey PRIMARY KEY (id),
  CONSTRAINT lead_assignment_history_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_assignment_history_from_user_id_fkey FOREIGN KEY (from_user_id) REFERENCES public.profiles(id),
  CONSTRAINT lead_assignment_history_to_user_id_fkey FOREIGN KEY (to_user_id) REFERENCES public.profiles(id),
  CONSTRAINT lead_assignment_history_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_attachments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid NOT NULL,
  storage_path text NOT NULL,
  file_name text NOT NULL,
  mime_type text,
  file_size_bytes bigint,
  attachment_type text,
  uploaded_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  legacy_attachment_id text,
  legacy_file_url text,
  CONSTRAINT lead_attachments_pkey PRIMARY KEY (id),
  CONSTRAINT lead_attachments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT lead_attachments_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_attachments_uploaded_by_fkey FOREIGN KEY (uploaded_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_compliance_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  compliance_item_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'submitted'::text, 'approved'::text, 'rejected'::text])),
  submitted_at timestamp with time zone,
  approved_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  organization_id uuid,
  document_id uuid,
  reviewer_user_id uuid,
  reviewed_at timestamp with time zone,
  review_notes text,
  due_at timestamp with time zone,
  blocked_stage text,
  severity text DEFAULT 'medium'::text,
  CONSTRAINT lead_compliance_items_pkey PRIMARY KEY (id),
  CONSTRAINT lead_compliance_items_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_compliance_items_compliance_item_id_fkey FOREIGN KEY (compliance_item_id) REFERENCES public.compliance_checklist_items(id),
  CONSTRAINT lead_compliance_items_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT lead_compliance_items_document_id_fkey FOREIGN KEY (document_id) REFERENCES public.documents(id),
  CONSTRAINT lead_compliance_items_reviewer_user_id_fkey FOREIGN KEY (reviewer_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_follow_ups (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid NOT NULL,
  assigned_user_id uuid,
  scheduled_at timestamp with time zone NOT NULL,
  status text NOT NULL DEFAULT 'scheduled'::text CHECK (status = ANY (ARRAY['pending'::text, 'scheduled'::text, 'completed'::text, 'missed'::text, 'cancelled'::text])),
  notes text,
  completed_at timestamp with time zone,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  legacy_follow_up_id text,
  CONSTRAINT lead_follow_ups_pkey PRIMARY KEY (id),
  CONSTRAINT lead_follow_ups_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT lead_follow_ups_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_follow_ups_assigned_user_id_fkey FOREIGN KEY (assigned_user_id) REFERENCES public.profiles(id),
  CONSTRAINT lead_follow_ups_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_markets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  market_id uuid NOT NULL,
  CONSTRAINT lead_markets_pkey PRIMARY KEY (id),
  CONSTRAINT lead_markets_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_markets_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id)
);
CREATE TABLE public.lead_playbook_runs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  playbook_id uuid NOT NULL,
  current_step_id uuid,
  status text NOT NULL DEFAULT 'in_progress'::text CHECK (status = ANY (ARRAY['in_progress'::text, 'completed'::text, 'cancelled'::text])),
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  CONSTRAINT lead_playbook_runs_pkey PRIMARY KEY (id),
  CONSTRAINT lead_playbook_runs_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_playbook_runs_playbook_id_fkey FOREIGN KEY (playbook_id) REFERENCES public.playbooks(id),
  CONSTRAINT lead_playbook_runs_current_step_id_fkey FOREIGN KEY (current_step_id) REFERENCES public.playbook_steps(id)
);
CREATE TABLE public.lead_product_interests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  product_id uuid,
  label text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT lead_product_interests_pkey PRIMARY KEY (id),
  CONSTRAINT lead_product_interests_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_product_interests_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.lead_scores (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  calculated_by uuid,
  score numeric NOT NULL CHECK (score >= 0::numeric),
  rationale text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT lead_scores_pkey PRIMARY KEY (id),
  CONSTRAINT lead_scores_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_scores_calculated_by_fkey FOREIGN KEY (calculated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_stage_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid NOT NULL,
  from_stage_id uuid,
  to_stage_id uuid,
  changed_by uuid,
  changed_at timestamp with time zone NOT NULL DEFAULT now(),
  note text,
  CONSTRAINT lead_stage_history_pkey PRIMARY KEY (id),
  CONSTRAINT lead_stage_history_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT lead_stage_history_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_stage_history_from_stage_id_fkey FOREIGN KEY (from_stage_id) REFERENCES public.pipeline_stages(id),
  CONSTRAINT lead_stage_history_to_stage_id_fkey FOREIGN KEY (to_stage_id) REFERENCES public.pipeline_stages(id),
  CONSTRAINT lead_stage_history_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.lead_tags (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lead_id uuid NOT NULL,
  tag_id uuid NOT NULL,
  CONSTRAINT lead_tags_pkey PRIMARY KEY (id),
  CONSTRAINT lead_tags_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT lead_tags_tag_id_fkey FOREIGN KEY (tag_id) REFERENCES public.tags(id)
);
CREATE TABLE public.leads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_type text NOT NULL CHECK (lead_type = ANY (ARRAY['buyer'::text, 'supplier'::text])),
  owner_user_id uuid,
  created_by uuid,
  updated_by uuid,
  company_name text NOT NULL,
  contact_name text,
  job_title text,
  email text,
  phone text,
  phone_secondary text,
  website text,
  social_handle text,
  country text,
  private_label_mode text,
  product_type text,
  products_or_needs text,
  ex_factory text,
  fob text,
  notes text,
  deal_value numeric,
  deal_currency text DEFAULT 'USD'::text,
  pipeline_id uuid,
  stage_id uuid,
  next_step_id uuid,
  source_type text,
  source_label text,
  trade_event_id uuid,
  intro_sent boolean NOT NULL DEFAULT false,
  last_contacted_at timestamp with time zone,
  next_follow_up_at timestamp with time zone,
  legacy_lead_id text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  market_id uuid,
  country_id uuid,
  phone_country_code text,
  phone_secondary_country_code text,
  CONSTRAINT leads_pkey PRIMARY KEY (id),
  CONSTRAINT leads_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id),
  CONSTRAINT leads_country_id_fkey FOREIGN KEY (country_id) REFERENCES public.countries(id),
  CONSTRAINT leads_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT leads_owner_user_id_fkey FOREIGN KEY (owner_user_id) REFERENCES public.profiles(id),
  CONSTRAINT leads_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT leads_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id),
  CONSTRAINT leads_pipeline_id_fkey FOREIGN KEY (pipeline_id) REFERENCES public.pipelines(id),
  CONSTRAINT leads_stage_id_fkey FOREIGN KEY (stage_id) REFERENCES public.pipeline_stages(id),
  CONSTRAINT leads_next_step_id_fkey FOREIGN KEY (next_step_id) REFERENCES public.next_steps(id),
  CONSTRAINT leads_trade_event_id_fkey FOREIGN KEY (trade_event_id) REFERENCES public.trade_events(id)
);
CREATE TABLE public.markets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  market_code text,
  CONSTRAINT markets_pkey PRIMARY KEY (id),
  CONSTRAINT markets_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.my_card_settings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  organization_id uuid,
  share_slug text NOT NULL UNIQUE,
  primary_phone text,
  secondary_phone text,
  website text,
  address text,
  booking_url text,
  quote_url text,
  linkedin_url text,
  instagram_url text,
  facebook_url text,
  tiktok_url text,
  is_public boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT my_card_settings_pkey PRIMARY KEY (id),
  CONSTRAINT my_card_settings_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT my_card_settings_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.next_steps (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT next_steps_pkey PRIMARY KEY (id),
  CONSTRAINT next_steps_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.organization_invitations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  email text NOT NULL,
  role_id uuid,
  invited_by_membership_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'pending'::text, 'sent'::text, 'accepted'::text, 'revoked'::text, 'expired'::text, 'failed'::text])),
  token_hash text,
  expires_at timestamp with time zone,
  last_sent_at timestamp with time zone,
  accepted_at timestamp with time zone,
  revoked_at timestamp with time zone,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_invitations_pkey PRIMARY KEY (id),
  CONSTRAINT organization_invitations_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_invitations_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(id),
  CONSTRAINT organization_invitations_invited_by_membership_id_fkey FOREIGN KEY (invited_by_membership_id) REFERENCES public.organization_members(id)
);
CREATE TABLE public.organization_members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  user_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_members_pkey PRIMARY KEY (id),
  CONSTRAINT organization_members_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_members_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.organizations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  logo_url text,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  default_currency text DEFAULT 'USD'::text,
  CONSTRAINT organizations_pkey PRIMARY KEY (id),
  CONSTRAINT organizations_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.pipeline_stages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  pipeline_id uuid NOT NULL,
  name text NOT NULL,
  sort_order integer NOT NULL DEFAULT 0,
  color text,
  is_closed boolean NOT NULL DEFAULT false,
  is_won boolean NOT NULL DEFAULT false,
  is_lost boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT pipeline_stages_pkey PRIMARY KEY (id),
  CONSTRAINT pipeline_stages_pipeline_id_fkey FOREIGN KEY (pipeline_id) REFERENCES public.pipelines(id)
);
CREATE TABLE public.pipelines (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  lead_type text NOT NULL CHECK (lead_type = ANY (ARRAY['buyer'::text, 'supplier'::text, 'both'::text])),
  is_default boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT pipelines_pkey PRIMARY KEY (id),
  CONSTRAINT pipelines_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.playbook_steps (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  playbook_id uuid NOT NULL,
  step_order integer NOT NULL,
  action text NOT NULL,
  parameters jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT playbook_steps_pkey PRIMARY KEY (id),
  CONSTRAINT playbook_steps_playbook_id_fkey FOREIGN KEY (playbook_id) REFERENCES public.playbooks(id)
);
CREATE TABLE public.playbooks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT playbooks_pkey PRIMARY KEY (id),
  CONSTRAINT playbooks_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT playbooks_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.pricing_engine_settings (
  organization_id uuid NOT NULL,
  default_display_currency text NOT NULL DEFAULT 'USD'::text,
  default_validity_days integer NOT NULL DEFAULT 7,
  default_fx_base_currency text NOT NULL DEFAULT 'USD'::text,
  allow_manual_fx boolean NOT NULL DEFAULT true,
  require_approval_for_override boolean NOT NULL DEFAULT false,
  approval_threshold_percent numeric,
  default_template_chips_id uuid,
  default_template_powders_id uuid,
  default_template_both_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT pricing_engine_settings_pkey PRIMARY KEY (organization_id),
  CONSTRAINT pricing_engine_settings_default_template_powders_id_fkey FOREIGN KEY (default_template_powders_id) REFERENCES public.quote_templates(id),
  CONSTRAINT pricing_engine_settings_default_template_both_id_fkey FOREIGN KEY (default_template_both_id) REFERENCES public.quote_templates(id),
  CONSTRAINT pricing_engine_settings_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT pricing_engine_settings_default_template_chips_id_fkey FOREIGN KEY (default_template_chips_id) REFERENCES public.quote_templates(id)
);
CREATE TABLE public.pricing_rule_sets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  description text,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['draft'::text, 'active'::text, 'archived'::text])),
  is_default boolean NOT NULL DEFAULT false,
  source_type text NOT NULL DEFAULT 'spreadsheet_import'::text CHECK (source_type = ANY (ARRAY['spreadsheet_import'::text, 'manual_admin'::text, 'api'::text])),
  source_reference text,
  created_by uuid,
  updated_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  source_file_name text,
  source_file_checksum text,
  imported_sheet_names jsonb NOT NULL DEFAULT '[]'::jsonb,
  fx_week_start date,
  fx_week_end date,
  fx_base_currency text NOT NULL DEFAULT 'USD'::text,
  fx_provider text,
  fx_snapshot_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  import_status text,
  validation_summary jsonb NOT NULL DEFAULT '{}'::jsonb,
  total_rows_processed integer NOT NULL DEFAULT 0,
  total_rows_imported integer NOT NULL DEFAULT 0,
  total_rows_failed integer NOT NULL DEFAULT 0,
  CONSTRAINT pricing_rule_sets_pkey PRIMARY KEY (id),
  CONSTRAINT pricing_rule_sets_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT pricing_rule_sets_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT pricing_rule_sets_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.product_categories (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  parent_id uuid,
  CONSTRAINT product_categories_pkey PRIMARY KEY (id),
  CONSTRAINT product_categories_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT product_categories_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.product_categories(id)
);
CREATE TABLE public.product_prices (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  product_variant_id uuid NOT NULL,
  market_id uuid NOT NULL,
  currency text NOT NULL,
  price numeric NOT NULL CHECK (price >= 0::numeric),
  effective_from date NOT NULL,
  effective_to date,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_prices_pkey PRIMARY KEY (id),
  CONSTRAINT product_prices_product_variant_id_fkey FOREIGN KEY (product_variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT product_prices_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id)
);
CREATE TABLE public.product_pricing_rules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  pricing_rule_set_id uuid NOT NULL,
  product_id uuid,
  product_variant_id uuid,
  sku_code text NOT NULL,
  hsn_code text,
  product_name text NOT NULL,
  category_type text NOT NULL CHECK (category_type = ANY (ARRAY['chips'::text, 'powders'::text])),
  pack_label text,
  units_per_case numeric,
  moq numeric,
  is_active boolean NOT NULL DEFAULT true,
  is_quoteable boolean NOT NULL DEFAULT true,
  ex_factory_usd numeric,
  fob_usd numeric,
  bulk_ex_factory_usd_per_kg numeric,
  ex_factory_inr numeric,
  fob_inr numeric,
  bulk_ex_factory_inr_per_kg numeric,
  effective_from date NOT NULL DEFAULT CURRENT_DATE,
  effective_to date,
  sort_order integer NOT NULL DEFAULT 0,
  raw_source_row_no integer,
  raw_source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  brand_name text,
  category_name text,
  pricing_type text,
  ex_factory_input_currency text,
  ex_factory_input_amount numeric,
  fob_input_currency text,
  fob_input_amount numeric,
  bulk_input_currency text,
  bulk_input_amount_per_kg numeric,
  ex_factory_usd_per_unit numeric,
  fob_usd_per_unit numeric,
  bulk_usd_per_kg numeric,
  ex_factory_usd_per_case numeric,
  fob_usd_per_case numeric,
  fx_reference_week_start date,
  fx_reference_week_end date,
  fx_provider text,
  fx_rate_to_usd numeric,
  fx_source_currency text,
  source_sheet_name text,
  source_hash text,
  created_by uuid,
  updated_by uuid,
  CONSTRAINT product_pricing_rules_pkey PRIMARY KEY (id),
  CONSTRAINT product_pricing_rules_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT product_pricing_rules_pricing_rule_set_id_fkey FOREIGN KEY (pricing_rule_set_id) REFERENCES public.pricing_rule_sets(id),
  CONSTRAINT product_pricing_rules_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT product_pricing_rules_product_variant_id_fkey FOREIGN KEY (product_variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT product_pricing_rules_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT product_pricing_rules_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.product_variants (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  product_id uuid NOT NULL,
  name text NOT NULL,
  hs_code_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  organization_id uuid,
  sku_code text,
  variant_code text,
  brand_name text,
  pack_size_value numeric CHECK (pack_size_value IS NULL OR pack_size_value > 0::numeric),
  pack_size_unit text,
  pack_label text,
  units_per_case numeric CHECK (units_per_case IS NULL OR units_per_case >= 0::numeric),
  moq_cases numeric CHECK (moq_cases IS NULL OR moq_cases >= 0::numeric),
  moq_kg numeric CHECK (moq_kg IS NULL OR moq_kg >= 0::numeric),
  pricing_mode_default text,
  supports_bulk_pricing boolean NOT NULL DEFAULT false,
  net_weight_kg numeric CHECK (net_weight_kg IS NULL OR net_weight_kg >= 0::numeric),
  hsn_code text,
  hsn_confidence_score numeric,
  hsn_source text,
  is_active boolean NOT NULL DEFAULT true,
  is_quoteable boolean NOT NULL DEFAULT true,
  sort_order integer NOT NULL DEFAULT 0,
  source_sheet_name text,
  source_row_no integer,
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT product_variants_pkey PRIMARY KEY (id),
  CONSTRAINT product_variants_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT product_variants_hs_code_id_fkey FOREIGN KEY (hs_code_id) REFERENCES public.hs_codes(id),
  CONSTRAINT product_variants_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT product_variants_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT product_variants_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.products (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  category_id uuid,
  name text NOT NULL,
  sku text,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  sku_code text,
  hsn_code text,
  brand_name text,
  pack_size text,
  supplier_name text,
  short_code text,
  pricing_type text,
  product_family_code text,
  sort_order integer NOT NULL DEFAULT 0,
  created_by uuid,
  updated_by uuid,
  CONSTRAINT products_pkey PRIMARY KEY (id),
  CONSTRAINT products_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT products_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.product_categories(id),
  CONSTRAINT products_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id),
  CONSTRAINT products_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  full_name text,
  email text,
  avatar_url text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  username text UNIQUE,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.quote_line_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_id uuid NOT NULL,
  product_id uuid,
  quantity numeric NOT NULL CHECK (quantity > 0::numeric),
  unit_price numeric CHECK (unit_price >= 0::numeric),
  currency text,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  product_variant_id uuid,
  catalog_price_id uuid,
  catalog_price_amount numeric,
  catalog_price_currency text,
  is_price_overridden boolean NOT NULL DEFAULT false,
  override_reason text,
  overridden_by uuid,
  overridden_at timestamp with time zone,
  CONSTRAINT quote_line_items_pkey PRIMARY KEY (id),
  CONSTRAINT quote_line_items_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT quote_line_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT quote_line_items_product_variant_id_fkey FOREIGN KEY (product_variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT quote_line_items_catalog_price_id_fkey FOREIGN KEY (catalog_price_id) REFERENCES public.product_prices(id),
  CONSTRAINT quote_line_items_overridden_by_fkey FOREIGN KEY (overridden_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.quote_negotiation_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_id uuid NOT NULL,
  quote_version_id uuid,
  event_type text NOT NULL CHECK (event_type = ANY (ARRAY['sent'::text, 'opened'::text, 'comment_added'::text, 'counter_offer'::text, 'line_override_requested'::text, 'line_override_approved'::text, 'line_override_rejected'::text, 'revision_created'::text, 'accepted'::text, 'rejected'::text, 'expired'::text])),
  actor_type text NOT NULL CHECK (actor_type = ANY (ARRAY['internal_user'::text, 'buyer_contact'::text, 'system'::text])),
  actor_user_id uuid,
  actor_name text,
  message text,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quote_negotiation_events_pkey PRIMARY KEY (id),
  CONSTRAINT quote_negotiation_events_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT quote_negotiation_events_quote_version_id_fkey FOREIGN KEY (quote_version_id) REFERENCES public.quote_versions(id),
  CONSTRAINT quote_negotiation_events_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.quote_pricing_snapshots (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_version_id uuid NOT NULL UNIQUE,
  pricing_rule_set_id uuid,
  freight_profile_id uuid,
  fx_base_currency text NOT NULL DEFAULT 'USD'::text,
  fx_display_currency text NOT NULL,
  fx_rate numeric,
  fx_provider text,
  fx_effective_at timestamp with time zone,
  quote_context jsonb NOT NULL DEFAULT '{}'::jsonb,
  freight_context jsonb NOT NULL DEFAULT '{}'::jsonb,
  calculation_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  source_hash text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quote_pricing_snapshots_pkey PRIMARY KEY (id),
  CONSTRAINT quote_pricing_snapshots_quote_version_id_fkey FOREIGN KEY (quote_version_id) REFERENCES public.quote_versions(id),
  CONSTRAINT quote_pricing_snapshots_pricing_rule_set_id_fkey FOREIGN KEY (pricing_rule_set_id) REFERENCES public.pricing_rule_sets(id),
  CONSTRAINT quote_pricing_snapshots_freight_profile_id_fkey FOREIGN KEY (freight_profile_id) REFERENCES public.freight_profiles(id)
);
CREATE TABLE public.quote_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  template_type text NOT NULL CHECK (template_type = ANY (ARRAY['chips'::text, 'powders'::text, 'both'::text])),
  is_default boolean NOT NULL DEFAULT false,
  header_config jsonb NOT NULL DEFAULT '{}'::jsonb,
  footer_config jsonb NOT NULL DEFAULT '{}'::jsonb,
  layout_schema jsonb NOT NULL DEFAULT '{}'::jsonb,
  is_active boolean NOT NULL DEFAULT true,
  created_by uuid,
  updated_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quote_templates_pkey PRIMARY KEY (id),
  CONSTRAINT quote_templates_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT quote_templates_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT quote_templates_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.quote_version_line_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_version_id uuid NOT NULL,
  product_id uuid,
  product_variant_id uuid,
  sku_code text NOT NULL,
  hsn_code text,
  product_name text NOT NULL,
  category_type text NOT NULL CHECK (category_type = ANY (ARRAY['chips'::text, 'powders'::text])),
  pack_label text,
  basis_applied text NOT NULL CHECK (basis_applied = ANY (ARRAY['ex_factory'::text, 'fob'::text, 'cif'::text, 'bulk_chips'::text])),
  pricing_mode text NOT NULL CHECK (pricing_mode = ANY (ARRAY['unit'::text, 'case'::text, 'kg'::text, 'bulk_kg'::text])),
  units_per_case numeric,
  moq numeric,
  source_ex_factory_usd numeric,
  source_fob_usd numeric,
  source_bulk_usd_per_kg numeric,
  source_ex_factory_inr numeric,
  source_fob_inr numeric,
  source_bulk_inr_per_kg numeric,
  freight_add_on_usd numeric,
  fx_rate numeric,
  final_unit_price numeric,
  final_case_price numeric,
  final_kg_price numeric,
  display_currency text NOT NULL CHECK (display_currency = ANY (ARRAY['USD'::text, 'INR'::text, 'EUR'::text, 'GBP'::text, 'AED'::text])),
  is_overridden boolean NOT NULL DEFAULT false,
  override_reason text,
  overridden_by uuid,
  overridden_at timestamp with time zone,
  line_notes text,
  sort_order integer NOT NULL DEFAULT 0,
  calculation_meta jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  catalog_pricing_rule_id uuid,
  catalog_pricing_rule_set_id uuid,
  override_requested_by uuid,
  override_type text,
  override_status text,
  catalog_price_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT quote_version_line_items_pkey PRIMARY KEY (id),
  CONSTRAINT quote_version_line_items_quote_version_id_fkey FOREIGN KEY (quote_version_id) REFERENCES public.quote_versions(id),
  CONSTRAINT quote_version_line_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT quote_version_line_items_product_variant_id_fkey FOREIGN KEY (product_variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT quote_version_line_items_overridden_by_fkey FOREIGN KEY (overridden_by) REFERENCES public.profiles(id),
  CONSTRAINT quote_vli_catalog_pricing_rule_id_fkey FOREIGN KEY (catalog_pricing_rule_id) REFERENCES public.product_pricing_rules(id),
  CONSTRAINT quote_vli_catalog_pricing_rule_set_id_fkey FOREIGN KEY (catalog_pricing_rule_set_id) REFERENCES public.pricing_rule_sets(id),
  CONSTRAINT quote_vli_override_requested_by_fkey FOREIGN KEY (override_requested_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.quote_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_id uuid NOT NULL,
  version_no integer NOT NULL,
  parent_version_id uuid,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'compiled'::text, 'approval_pending'::text, 'approved'::text, 'sent'::text, 'viewed'::text, 'customer_countered'::text, 'superseded'::text, 'accepted'::text, 'rejected'::text, 'expired'::text, 'cancelled'::text])),
  pricing_basis text NOT NULL CHECK (pricing_basis = ANY (ARRAY['ex_factory'::text, 'fob'::text, 'cif'::text, 'bulk_chips'::text])),
  display_currency text NOT NULL CHECK (display_currency = ANY (ARRAY['USD'::text, 'INR'::text, 'EUR'::text, 'GBP'::text, 'AED'::text])),
  valid_until date,
  customer_message text,
  internal_notes text,
  sent_at timestamp with time zone,
  sent_by uuid,
  approved_at timestamp with time zone,
  approved_by uuid,
  pdf_document_id uuid,
  total_line_count integer NOT NULL DEFAULT 0,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  import_run_id uuid,
  source_file_name text,
  source_hash text,
  CONSTRAINT quote_versions_pkey PRIMARY KEY (id),
  CONSTRAINT quote_versions_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT quote_versions_parent_version_id_fkey FOREIGN KEY (parent_version_id) REFERENCES public.quote_versions(id),
  CONSTRAINT quote_versions_sent_by_fkey FOREIGN KEY (sent_by) REFERENCES public.profiles(id),
  CONSTRAINT quote_versions_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.profiles(id),
  CONSTRAINT quote_versions_pdf_document_id_fkey FOREIGN KEY (pdf_document_id) REFERENCES public.documents(id),
  CONSTRAINT quote_versions_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT quote_versions_import_run_id_fkey FOREIGN KEY (import_run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.quotes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  rfq_id uuid,
  lead_id uuid NOT NULL,
  created_by uuid,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'in_review'::text, 'sent'::text, 'negotiating'::text, 'accepted'::text, 'rejected'::text, 'expired'::text, 'cancelled'::text])),
  currency text,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  quote_number text,
  version_no integer NOT NULL DEFAULT 0,
  pricing_basis text CHECK (pricing_basis IS NULL OR (pricing_basis = ANY (ARRAY['ex_factory'::text, 'fob'::text, 'cif'::text, 'bulk_chips'::text]))),
  display_currency text CHECK (display_currency IS NULL OR (display_currency = ANY (ARRAY['USD'::text, 'INR'::text, 'EUR'::text, 'GBP'::text, 'AED'::text]))),
  destination_port text,
  valid_until date,
  market_id uuid,
  country_id uuid,
  freight_profile_id uuid,
  current_version_id uuid,
  source_type text NOT NULL DEFAULT 'manual'::text CHECK (source_type = ANY (ARRAY['manual'::text, 'rfq'::text, 'lead'::text, 'imported'::text, 'api'::text])),
  approval_required boolean NOT NULL DEFAULT false,
  approved_at timestamp with time zone,
  approved_by uuid,
  accepted_version_id uuid,
  notes_internal text,
  notes_customer text,
  import_run_id uuid,
  source_file_name text,
  source_hash text,
  CONSTRAINT quotes_pkey PRIMARY KEY (id),
  CONSTRAINT quotes_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT quotes_rfq_id_fkey FOREIGN KEY (rfq_id) REFERENCES public.rfqs(id),
  CONSTRAINT quotes_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT quotes_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT quotes_market_id_fkey FOREIGN KEY (market_id) REFERENCES public.markets(id),
  CONSTRAINT quotes_country_id_fkey FOREIGN KEY (country_id) REFERENCES public.countries(id),
  CONSTRAINT quotes_freight_profile_id_fkey FOREIGN KEY (freight_profile_id) REFERENCES public.freight_profiles(id),
  CONSTRAINT quotes_current_version_id_fkey FOREIGN KEY (current_version_id) REFERENCES public.quote_versions(id),
  CONSTRAINT quotes_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.profiles(id),
  CONSTRAINT quotes_accepted_version_id_fkey FOREIGN KEY (accepted_version_id) REFERENCES public.quote_versions(id),
  CONSTRAINT quotes_import_run_id_fkey FOREIGN KEY (import_run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.rate_limit_hits (
  key text NOT NULL,
  count integer NOT NULL DEFAULT 1,
  window_start timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT rate_limit_hits_pkey PRIMARY KEY (key)
);
CREATE TABLE public.rfq_line_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  rfq_id uuid,
  product_id uuid,
  quantity numeric NOT NULL,
  unit_price numeric,
  currency text,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  product_variant_id uuid,
  catalog_price_id uuid,
  catalog_price_amount numeric,
  catalog_price_currency text,
  is_price_overridden boolean NOT NULL DEFAULT false,
  override_reason text,
  overridden_by uuid,
  overridden_at timestamp with time zone,
  CONSTRAINT rfq_line_items_pkey PRIMARY KEY (id),
  CONSTRAINT rfq_line_items_rfq_id_fkey FOREIGN KEY (rfq_id) REFERENCES public.rfqs(id),
  CONSTRAINT rfq_line_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT rfq_line_items_product_variant_id_fkey FOREIGN KEY (product_variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT rfq_line_items_catalog_price_id_fkey FOREIGN KEY (catalog_price_id) REFERENCES public.product_prices(id),
  CONSTRAINT rfq_line_items_overridden_by_fkey FOREIGN KEY (overridden_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.rfqs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid,
  created_by uuid,
  status text NOT NULL DEFAULT 'draft'::text,
  validity_date date,
  currency text,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT rfqs_pkey PRIMARY KEY (id),
  CONSTRAINT rfqs_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT rfqs_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT rfqs_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.role_permissions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  role_id uuid NOT NULL,
  permission text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT role_permissions_pkey PRIMARY KEY (id),
  CONSTRAINT role_permissions_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(id)
);
CREATE TABLE public.roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid,
  name text NOT NULL,
  description text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT roles_pkey PRIMARY KEY (id),
  CONSTRAINT roles_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.saved_views (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  entity_type text NOT NULL CHECK (entity_type = ANY (ARRAY['leads'::text, 'accounts'::text, 'pipeline'::text, 'rfqs'::text, 'quotes'::text, 'products'::text, 'catalog'::text, 'documents'::text, 'compliance'::text, 'tasks'::text, 'trade_events'::text, 'integrations'::text, 'ai_suggestions'::text])),
  name text NOT NULL,
  description text,
  visibility text NOT NULL DEFAULT 'private'::text CHECK (visibility = ANY (ARRAY['private'::text, 'team'::text, 'org'::text])),
  filter_model jsonb NOT NULL DEFAULT '{}'::jsonb,
  sort_model jsonb,
  column_model jsonb,
  created_by_membership_id uuid NOT NULL,
  updated_by_membership_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT saved_views_pkey PRIMARY KEY (id),
  CONSTRAINT saved_views_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT saved_views_created_by_membership_id_fkey FOREIGN KEY (created_by_membership_id) REFERENCES public.organization_members(id),
  CONSTRAINT saved_views_updated_by_membership_id_fkey FOREIGN KEY (updated_by_membership_id) REFERENCES public.organization_members(id)
);
CREATE TABLE public.scheduled_tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  lead_id uuid,
  task_type text NOT NULL,
  payload jsonb NOT NULL,
  scheduled_for timestamp with time zone NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'in_progress'::text, 'completed'::text, 'failed'::text, 'cancelled'::text])),
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  CONSTRAINT scheduled_tasks_pkey PRIMARY KEY (id),
  CONSTRAINT scheduled_tasks_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT scheduled_tasks_lead_id_fkey FOREIGN KEY (lead_id) REFERENCES public.leads(id),
  CONSTRAINT scheduled_tasks_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.stg_lead_raw (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  row_hash text,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  parse_status text NOT NULL DEFAULT 'pending'::text CHECK (parse_status = ANY (ARRAY['pending'::text, 'parsed'::text, 'error'::text])),
  parse_error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_lead_raw_pkey PRIMARY KEY (id),
  CONSTRAINT stg_lead_raw_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_lead_resolved (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  row_hash text,
  resolved_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  validation_status text NOT NULL DEFAULT 'pending'::text CHECK (validation_status = ANY (ARRAY['pending'::text, 'valid'::text, 'valid_with_warning'::text, 'blocked'::text])),
  ready_to_insert boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_lead_resolved_pkey PRIMARY KEY (id),
  CONSTRAINT stg_lead_resolved_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_pricing_raw (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  row_hash text,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  parse_status text NOT NULL DEFAULT 'pending'::text CHECK (parse_status = ANY (ARRAY['pending'::text, 'parsed'::text, 'error'::text])),
  parse_error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_pricing_raw_pkey PRIMARY KEY (id),
  CONSTRAINT stg_pricing_raw_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_pricing_resolved (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  row_hash text,
  resolved_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  validation_status text NOT NULL DEFAULT 'pending'::text CHECK (validation_status = ANY (ARRAY['pending'::text, 'valid'::text, 'valid_with_warning'::text, 'blocked'::text])),
  ready_to_insert boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_pricing_resolved_pkey PRIMARY KEY (id),
  CONSTRAINT stg_pricing_resolved_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_product_raw (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  row_hash text,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  parse_status text NOT NULL DEFAULT 'pending'::text CHECK (parse_status = ANY (ARRAY['pending'::text, 'parsed'::text, 'error'::text])),
  parse_error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_product_raw_pkey PRIMARY KEY (id),
  CONSTRAINT stg_product_raw_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_product_resolved (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_sheet_name text,
  source_row_no integer,
  row_hash text,
  resolved_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  validation_status text NOT NULL DEFAULT 'pending'::text CHECK (validation_status = ANY (ARRAY['pending'::text, 'valid'::text, 'valid_with_warning'::text, 'blocked'::text])),
  ready_to_insert boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_product_resolved_pkey PRIMARY KEY (id),
  CONSTRAINT stg_product_resolved_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_quote_line_raw (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_page_ref text,
  source_line_no integer,
  row_hash text,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  parse_status text NOT NULL DEFAULT 'pending'::text CHECK (parse_status = ANY (ARRAY['pending'::text, 'parsed'::text, 'error'::text])),
  parse_error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_quote_line_raw_pkey PRIMARY KEY (id),
  CONSTRAINT stg_quote_line_raw_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_quote_line_resolved (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  quote_number_final text,
  source_page_ref text,
  source_line_no integer,
  row_hash text,
  resolved_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  validation_status text NOT NULL DEFAULT 'pending'::text CHECK (validation_status = ANY (ARRAY['pending'::text, 'valid'::text, 'valid_with_warning'::text, 'blocked'::text])),
  ready_to_insert boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_quote_line_resolved_pkey PRIMARY KEY (id),
  CONSTRAINT stg_quote_line_resolved_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_quote_raw (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  source_page_ref text,
  row_hash text,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  parse_status text NOT NULL DEFAULT 'pending'::text CHECK (parse_status = ANY (ARRAY['pending'::text, 'parsed'::text, 'error'::text])),
  parse_error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_quote_raw_pkey PRIMARY KEY (id),
  CONSTRAINT stg_quote_raw_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.stg_quote_resolved (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  run_id uuid NOT NULL,
  source_file_name text,
  quote_number_final text,
  resolved_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  validation_status text NOT NULL DEFAULT 'pending'::text CHECK (validation_status = ANY (ARRAY['pending'::text, 'valid'::text, 'valid_with_warning'::text, 'blocked'::text])),
  ready_to_insert boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stg_quote_resolved_pkey PRIMARY KEY (id),
  CONSTRAINT stg_quote_resolved_run_id_fkey FOREIGN KEY (run_id) REFERENCES public.import_runs(id)
);
CREATE TABLE public.tags (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  color text,
  CONSTRAINT tags_pkey PRIMARY KEY (id),
  CONSTRAINT tags_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.trade_event_entries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  trade_event_id uuid NOT NULL,
  captured_company_name text,
  captured_contact_name text,
  captured_job_title text,
  captured_email text,
  captured_phone text,
  captured_country text,
  captured_notes text,
  source_label text,
  source_scan_ref text,
  status text NOT NULL DEFAULT 'new'::text CHECK (status = ANY (ARRAY['new'::text, 'qualified'::text, 'converted'::text, 'duplicate'::text, 'discarded'::text])),
  duplicate_of_entry_id uuid,
  converted_lead_id uuid,
  assigned_user_id uuid,
  normalized_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  raw_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  captured_at timestamp with time zone NOT NULL DEFAULT now(),
  qualified_at timestamp with time zone,
  converted_at timestamp with time zone,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT trade_event_entries_pkey PRIMARY KEY (id),
  CONSTRAINT trade_event_entries_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT trade_event_entries_trade_event_id_fkey FOREIGN KEY (trade_event_id) REFERENCES public.trade_events(id),
  CONSTRAINT trade_event_entries_duplicate_of_entry_id_fkey FOREIGN KEY (duplicate_of_entry_id) REFERENCES public.trade_event_entries(id),
  CONSTRAINT trade_event_entries_converted_lead_id_fkey FOREIGN KEY (converted_lead_id) REFERENCES public.leads(id),
  CONSTRAINT trade_event_entries_assigned_user_id_fkey FOREIGN KEY (assigned_user_id) REFERENCES public.profiles(id),
  CONSTRAINT trade_event_entries_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.trade_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name text NOT NULL,
  city text,
  country text,
  starts_on date,
  ends_on date,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT trade_events_pkey PRIMARY KEY (id),
  CONSTRAINT trade_events_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.user_roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_member_id uuid,
  role_id uuid,
  assigned_at timestamp with time zone DEFAULT now(),
  CONSTRAINT user_roles_pkey PRIMARY KEY (id),
  CONSTRAINT user_roles_organization_member_id_fkey FOREIGN KEY (organization_member_id) REFERENCES public.organization_members(id),
  CONSTRAINT user_roles_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(id)
);
CREATE TABLE public.view_preferences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  organization_member_id uuid NOT NULL,
  entity_type text NOT NULL CHECK (entity_type = ANY (ARRAY['leads'::text, 'accounts'::text, 'pipeline'::text, 'rfqs'::text, 'quotes'::text, 'products'::text, 'catalog'::text, 'documents'::text, 'compliance'::text, 'tasks'::text, 'trade_events'::text, 'integrations'::text, 'ai_suggestions'::text])),
  saved_view_id uuid,
  built_in_view_key text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT view_preferences_pkey PRIMARY KEY (id),
  CONSTRAINT view_preferences_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT view_preferences_organization_member_id_fkey FOREIGN KEY (organization_member_id) REFERENCES public.organization_members(id),
  CONSTRAINT view_preferences_saved_view_id_fkey FOREIGN KEY (saved_view_id) REFERENCES public.saved_views(id)
);