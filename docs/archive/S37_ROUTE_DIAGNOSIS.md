# S37 route diagnosis

The old inline lead workspace remains reachable from the lead queue because the /leads list keeps local activeView state and opens InlineLeadWorkspace instead of navigating to /leads/[leadId].
